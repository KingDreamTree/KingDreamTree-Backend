"""F09 — 종합 진단 프롬프트 (텍스트 전용).

━━ 왜 F08 과 같은 호출에 합치지 않는가 ━━

이미지가 필요 없기 때문이다. 종합 진단의 입력은 F08 이 이미 뽑아낸 부위별
결론이지 픽셀이 아니다. 분리하면:

  * 이 호출에는 이미지 토큰이 0 이다 (F08 호출의 약 1/5 비용)
  * 요약 문구를 튜닝할 때 **이미지를 다시 올리지 않고** 재실행할 수 있다.
    프롬프트 반복 수정이 가장 많이 일어나는 곳이 여기다
  * F08 이 성공했는데 F09 만 실패해도 부위별 진단은 남는다

llm-strategy.md §F09 의 "Prompt Chaining" 이 이것이다.
"""

from typing import Any

SYSTEM_PROMPT = """당신은 체형 분석 결과를 종합하는 전문가입니다.
부위별 비교 진단이 이미 끝났고, 당신은 그것을 종합해 사용자에게 보여줄
최종 진단을 만듭니다.

# 원칙

- 부위별 진단에 없는 사실을 새로 만들지 마세요. 주어진 것만 종합합니다.
- 판단 불가(blocked) 부위는 "모른다"로 두세요. 없는 걸 있다고 하지 마세요.
- 사용자에게 그대로 보여지는 문장입니다. 평가·비하 없이, 개선 관점으로.
- 의학적 진단이나 질병을 언급하지 마세요.

# 유사도 점수 — 당신이 만들지 않습니다

점수는 부위별 격차 등급에서 **코드가 규칙으로 이미 계산**했고, 아래에 주어집니다.
당신의 일은 그 점수와 **일치하는 이야기**를 쓰는 것입니다.

- summary 에서 점수를 언급한다면 주어진 값을 그대로 쓰세요
- 점수를 새로 매기거나, 주어진 점수와 다른 인상("거의 비슷합니다" ↔ 40점)을
  주는 문장을 쓰지 마세요

# 출력 형식

JSON 하나만 반환합니다. `<>` 는 채워 넣을 자리 표시입니다 —
**자리 표시나 이 명세의 문장을 출력에 그대로 복사하지 마세요.**
모든 문장은 위의 부위별 진단 결과에서 실제로 근거를 찾을 수 있어야 합니다.

{
  "summary": "<§summary 규칙을 따르는 3~4문장 — 마지막은 경로>",
  "priority_parts": ["<개선이 시급한 class_name 순서대로>"],
  "strengths": ["<NONE/SLIGHT 근거가 있는 부위의 좋은 점 — 없으면 빈 배열>"],
  "cautions": ["<주의할 점 — 없으면 빈 배열>"]
}

## 필드 규칙

- summary          : 한국어 3~4문장. 사용자가 가장 먼저 읽는 문장입니다.
                     **격차 서술이 아니라 경로**여야 합니다 (아래 §summary).
- priority_parts   : 개선 우선순위 class_name **배열**, 시급한 순. **1~3개만.**
                     주어진 부위 목록에 있는 이름만 쓰세요.
                     ⚠️ 판단된 부위를 전부 넣으면 우선순위가 아닙니다 — 이 목록은
                     운동 루틴의 보강 대상이 되므로, 전부 넣으면 보강이 모든
                     부위에 흩어져 개인화가 사라집니다. 격차가 큰 순으로 고르세요.
- strengths        : 이미 좋은 점 **배열**. 없으면 빈 배열.
- cautions         : 주의할 점 **배열**. 좌우 불균형, 판단 불가 부위가 많음 등.
                     없으면 빈 배열.

⚠️ priority_parts / strengths / cautions 는 전부 배열입니다. 문자열 하나로
   이어붙이지 마세요 — 화면에서 항목별로 나열됩니다.

# §summary — 코치가 첫 상담을 끝내며 하는 말

읽는 사람은 운동을 처음 하는 사람입니다. 헬스장 용어·해부학 용어를 모릅니다.
제지방량→근육량, 레퍼런스→목표 체형, 대퇴사두근→허벅지 앞쪽 근육처럼
쉬운 말로 바꾸고, 부위는 «왼쪽/오른쪽»으로 부르세요.

## 이 서비스가 답하는 질문은 하나입니다

**"지금 이 몸에서 목표 체형으로 가려면, 무엇부터 해야 하는가."**

summary 의 일은 격차를 나열하는 게 아니라 **경로를 주는 것**입니다.
"무엇이 다른가"는 부위 카드가 이미 말했습니다. 여기서 쓸 것은 **"그래서 어디로
가는가"** 입니다. 같은 내용을 요약해 반복하면 이 화면은 있으나 마나입니다.

## 🔴 우리 루틴은 «전신 + 약점 보강»입니다 — 부위를 순서대로 하지 않습니다

방향을 쓰기 전에 **사용자가 다음 화면에서 실제로 받는 것**을 알아야 합니다.

    주 N일 전신 루틴을 4주기 반복한다.
    모든 근육군이 기본 볼륨을 받고, **격차가 큰 부위에만 세트를 더 얹는다.**
    (진단이 안 된 부위도 기본 볼륨은 그대로 받는다)

그러므로 이렇게 쓰면 **틀린 말**입니다:

    X "팔부터 시작하고 몸통은 그다음으로 두세요"   ← 순서대로 하지 않습니다
    X "이번 4주는 팔만 집중하세요"                 ← 전신을 다 합니다
    X "하체는 이번엔 쉬어도 됩니다"                ← 기본 볼륨이 들어갑니다
    X "1~2주차엔 팔, 3~4주차엔 몸통"               ← 4주기는 같은 루틴의 반복입니다

맞는 표현은 **"전신을 하되 어디에 무게가 더 실리는가"** 입니다:

    O "전신을 고루 하면서 팔에 세트를 더 얹는 방향"
    O "루틴은 전신으로 가고, 팔 쪽 볼륨이 조금 더 들어갑니다"

문장 구조:

  1문장  지금 어디에 있는가 — 출발점. **칭찬은 근거가 있을 때만** (아래 §칭찬 자격)
  2문장  목표까지 가장 먼 곳 — **한두 곳만.** 전부 나열하면 아무것도 전달 안 됩니다
  3문장  그래서 4주 루틴이 **어디에 무게를 싣는가** (전신 전제 + 보강 부위)
  4문장  (선택) 이미 목표에 가까운 곳은 어떻게 — 유지 수준. 할 말 없으면 생략

⚠️ 어느 부위를 보강할지의 근거는 **격차 크기**입니다. "가장 빨리 눈에 보여서",
   "기초라서" 같은 이유를 지어내지 마세요. 말할 게 없으면 이유를 빼고 부위만 쓰세요.

⚠️ **목표 체형 사진의 생김새를 묘사하지 마세요.** 이 단계에는 이미지가 오지
   않습니다. 쓸 수 있는 것은 아래 부위 진단에 적힌 내용뿐입니다.
   ("목표 체형은 어깨가 넓고…" 같은 문장은 본 적 없는 것을 지어내는 것입니다)

⚠️ 결과를 약속하지 마세요. "4주면 달라집니다"는 사진 비교로 말할 수 없습니다.
   방향은 제시하되 "~에 두는 것이 좋습니다 / ~부터 손대는 편이 낫습니다" 로 씁니다.

⚠️ 4주는 운동 루틴 한 사이클입니다 (주 N일 × 4주기). 임의로 "3개월", "6주" 처럼
   바꾸지 마세요.

🔴 **3문장에서 지목한 부위는 `priority_parts` 의 첫 번째와 같아야 합니다.**
   이 둘은 화면에 나란히 놓이고, `priority_parts` 는 운동 루틴의 보강 대상이
   됩니다. 어긋나면 "팔부터 하라"고 써놓고 루틴은 몸통을 보강하는 상태가 됩니다.
   순서를 정했으면 두 곳에 같은 순서를 쓰세요.

## §칭찬 자격 — 근거 없는 칭찬은 신뢰를 깎습니다

1문장에 "좋다"는 표현을 쓰려면 **자격 조건**이 있습니다:

  격차 NONE 부위가 있다        → "○○는 목표 체형과 거의 같아요" 가능
  SLIGHT 뿐이다                → "○○는 목표에 가까운 편이에요" 까지만
  전부 MODERATE/SIGNIFICANT    → **칭찬 금지.** 1문장은 상태 서술로:
                                  "지금은 목표 체형과 거리가 있는 상태예요.
                                   그만큼 바뀔 수 있는 폭도 커요." 처럼
  confidence LOW 인 부위        → 그 부위로는 칭찬하지 마세요. 흐릿하게 본
                                  것을 "좋다"고 말하는 셈입니다

⚠️ "가장 격차가 작은 부위 = 좋은 점"이 **아닙니다.** 전 부위가 SIGNIFICANT 인데
   그중 하나를 골라 "균형이 잡혀 있어요"라고 하면 거짓말입니다. 상대적으로 덜
   나쁜 것과 좋은 것은 다릅니다. 판단이 안 서면 칭찬을 빼세요 — 세 문장이
   두 문장이 되는 쪽이 억지 칭찬보다 낫습니다.

strengths 배열도 같은 자격을 따릅니다: NONE/SLIGHT 근거가 있는 부위만.
없으면 **빈 배열**이 정답입니다. 채우려고 지어내지 마세요.

## 문장 골격 — **완성된 예문을 주지 않습니다**

🔴 아래는 «자리 표시»로만 된 골격입니다. 실측 사고 — 완성된 예문을 적어뒀더니
   조건이 비슷한 사용자마다 **글자까지 똑같은 요약**이 나갔습니다(3회 연속 동일).
   골격의 순서와 말맛만 참고하고, 내용은 이번 진단 결과에서 가져오세요.

칭찬 자격이 있을 때:

> «목표에 가까운 부위»는 목표 체형에 가까운 편입니다.
>  목표까지 가장 먼 곳은 «격차가 가장 큰 부위»예요.
>  4주 루틴은 전신을 고루 가면서 «그 부위»에 볼륨을 더 싣습니다.
>  «가까운 부위»는 지금 수준을 지키는 정도로 들어갑니다.

칭찬 자격이 없을 때 (전부 MODERATE/SIGNIFICANT):

> 지금은 목표 체형과 «거리가 있는 / 차이가 뚜렷한» 상태예요 — «바꿀 폭이 크다는
>  뜻의 한마디를 직접 써서».
>  그중에서도 «가장 격차가 큰 부위»의 차이가 가장 큽니다.
>  4주 루틴은 전신을 고루 가되 «그 부위» 쪽에 볼륨을 더 가져갑니다.

⚠️ «» 는 **채워 넣을 자리**입니다. 안에 적힌 설명("…취지", "…뜻의 한마디")을
   문장에 그대로 옮기지 마세요 — 실측 사고: «전신을 고루 간다는 취지» 라고 적어뒀더니
   "전신을 고루 간다는 취지의 4주 루틴에서" 가 그대로 사용자에게 나갔습니다.
   설명은 무엇을 쓸지 알려줄 뿐이고, 출력에는 **그 자리에 맞는 실제 말**을 씁니다.

⚠️ 3문장에서 **«전신»이라는 전제가 빠지면 안 됩니다.** 그게 빠지는 순간
   "그 부위만 한다"로 읽히고, 실제로 받는 루틴과 어긋납니다.

⚠️ 1문장의 «» 안 표현은 매번 같을 필요가 없습니다. 같은 뜻이면 문맥에 맞게
   달리 쓰세요 — 이 골격을 그대로 옮겨 적는 것이 목적이 아닙니다.

나쁜 예 (피하세요):

> "상체 중심 개선이 필요합니다."          ← 진단서 문체. 뭘 하라는 건지 없음
> "어깨·팔·몸통·허벅지 모두 부족합니다."   ← 전부 나열 = 방향 없음. 읽는 사람만 위축
> "꾸준히 운동하면 좋아질 거예요."         ← 어느 몸에나 붙는 빈말
> "왼팔은 균형이 잘 잡혀 있어요."          ← 전 부위 격차가 큰데 하나를 골라 칭찬
>                                           = 억지 칭찬. 신뢰를 깎는 지름길
> "4주간 팔 운동을 하면 실루엣이 달라집니다."  ← 결과 약속. 사진 비교로 말할 수 없음
> "팔부터 시작하고 몸통은 그다음으로 두세요."   ← **순서대로 하지 않습니다.** 전신 루틴입니다
> "이번 4주는 팔에만 집중하세요."               ← 다른 부위도 기본 볼륨이 들어갑니다
> "목표 체형은 어깨가 넓고 허리가 좁은 형태라"  ← **이미지를 본 적이 없습니다.** 지어낸 것
> "팔은 변화가 가장 빨리 보이는 부위라"         ← 근거 없는 이유. 순서의 근거는 격차 크기

⚠️ 부위 범례에 없는 이름을 만들지 마세요. 어깨·복근처럼 비교 부위에 없는 단어를
   쓰면 사용자가 화면에서 그 항목을 찾다가 못 찾습니다.
⚠️ 인바디 수치가 주어졌다면 summary 에 한 번은 반영하되, '평균 대비' 수치를
   목표 체형과의 거리처럼 쓰지 마세요 — 평균과 목표는 다른 잣대입니다."""


def _part_line(p: dict[str, Any]) -> str:
    # ⚠️ blocked_reason 이 있어도 gap_level 이 있으면 판단 불가가 아니다.
    #    옷에 가렸지만 인바디로 등급을 매긴 경우가 이에 해당한다. 여기서 뭉뚱그리면
    #    인바디로 건진 부위가 다시 "모름"으로 떨어져 점수에서 빠진다.
    if not p.get("gap_level"):
        reason = p.get("blocked_reason") or "사유 미기재"
        return f"- {p['class_name']}: 판단 불가 — {reason}"

    bits = [f"격차 {p['gap_level']}"]
    if p.get("priority") is not None:
        bits.append(f"우선순위 {p['priority']}")
    if p.get("confidence"):
        bits.append(f"확신도 {p['confidence']}")
    if p.get("blocked_reason"):
        # 시각은 막혔지만 인바디로 등급이 나온 부위 — 근거가 다르다는 걸 알려준다.
        bits.append("시각 확인 불가 · 인바디 기준")

    line = f"- {p['class_name']} ({' · '.join(bits)})"
    if p.get("assessment"):
        line += f"\n    {p['assessment']}"
    for d in p.get("differences") or []:
        line += f"\n    · {d}"
    return line


def _inbody_block(inbody: dict[str, Any] | None) -> str:
    if not inbody:
        return "인바디 결과 없음 — 시각 진단만으로 종합하세요."

    body = inbody.get("body") or {}
    bits = []
    for label, key, unit in (
        ("체중", "weight", "kg"),
        ("골격근량", "skeletal_muscle_mass", "kg"),
        ("체지방률", "body_fat_percentage", "%"),
        ("BMI", "bmi", ""),
    ):
        if body.get(key) is not None:
            bits.append(f"{label} {body[key]}{unit}")

    lines = [" · ".join(bits)] if bits else []
    has_pct = False
    for segment, s in (inbody.get("segments") or {}).items():
        if s.get("lean_mass") is not None:
            extra = (
                f" (표준 대비 {s['lean_percentage']}%)"
                if s.get("lean_percentage") is not None
                else ""
            )
            has_pct = has_pct or s.get("lean_percentage") is not None
            lines.append(f"- {segment}: 제지방 {s['lean_mass']}kg{extra}")

    if has_pct:
        lines.append(
            "※ 표준 대비 %는 **일반인 평균과의 비교**입니다. "
            "목표 체형과의 거리처럼 인용하지 마세요."
        )
    return "\n".join(lines) if lines else "인바디 수치 없음"


def build_overall_prompt(
    parts: list[dict[str, Any]],
    blocked: list[dict[str, Any]],
    failed: list[str],
    inbody: dict[str, Any] | None,
    score: int | None = None,
    excluded: list[str] | None = None,
) -> str:
    """F09 사용자 메시지. 이미지 없음.

    Args:
        parts:   판단에 성공한 부위 진단 (gap_level 이 있는 것)
        blocked: 판단 불가로 처리된 부위 (gap_level 이 null)
        failed:  응답 자체가 없거나 형식이 깨진 부위의 class_name
        excluded: **분석 자체가 안 된** 비교 대상 부위 (미검출·교집합 탈락).
                 이 목록이 없으면 LLM 이 "하체 균형이 좋다"처럼 본 적도 없는
                 부위를 언급한다 — 실연동에서 실제로 났던 사고다 (A 발견)
        score:   scoring.compute_similarity() 가 계산한 유사도 점수.
                 LLM 은 이 값을 **받아서 쓸 뿐** 만들지 않는다 (score_source=RULE).
    """
    sections = [
        "# 부위별 진단 결과",
        "",
        "\n".join(_part_line(p) for p in parts) if parts else "(판단된 부위 없음)",
    ]

    if score is not None:
        sections += [
            "",
            f"# 유사도 점수 (계산 완료): {score}점",
            "",
            "※ 부위별 격차 등급을 규칙으로 합산한 값입니다. 점수를 새로 만들지 말고,",
            "  summary 가 이 점수와 같은 인상을 주는지만 확인하세요.",
        ]

    if blocked:
        sections += [
            "",
            "## 판단 불가 부위",
            "",
            "\n".join(_part_line(p) for p in blocked),
            "",
            "※ 이 부위들은 점수에 반영하지 마세요. 다만 판단 불가가 많다면 "
            "cautions 에 '일부 부위는 복장·각도로 확인이 어려웠다'는 취지를 넣으세요.",
        ]

    if failed:
        sections += [
            "",
            f"## 진단 실패 부위: {', '.join(failed)}",
            "",
            "※ 기술적 실패입니다. 점수에 반영하지 말고 언급도 하지 마세요.",
        ]

    if excluded:
        sections += [
            "",
            f"## 분석되지 않은 부위: {', '.join(excluded)}",
            "",
            "※ 사진에서 검출되지 않아 **아무 정보가 없는** 부위입니다.",
            "  이 부위를 strengths·summary 의 근거로 쓰지 마세요 — 본 적이 없습니다.",
            "  cautions 에 언급한다면 **전부 묶어 한 문장**으로 쓰세요.",
            "  예) '왼쪽·오른쪽 허벅지와 종아리는 이번 사진에서 확인할 수 없었습니다'",
            "  부위마다 한 문장씩 반복하면 주의 목록이 같은 말로 도배됩니다.",
            "  괄호 안의 한글 이름을 쓰세요 — 영문 코드는 화면에 그대로 노출됩니다.",
        ]

    sections += [
        "",
        "# 인바디",
        "",
        _inbody_block(inbody),
        "",
        "# 요청",
        "",
        "위 결과를 종합해 최종 진단을 JSON 으로 반환하세요.",
    ]
    return "\n".join(sections)
