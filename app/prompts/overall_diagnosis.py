"""F09 — 종합 진단 프롬프트. 원본 사진 두 장 + 부위별 진단 결과를 받는다.

━━ 왜 이미지를 받게 됐나 (2026-08-17 변경) ━━

부위 9개를 각각 정확히 봐도 **부위 사이의 관계**는 어느 카드에도 안 나온다.
"상체는 목표에 가까운데 하체가 얇아 위아래가 안 맞는다", "어깨 폭 대비 허리가
넓다" 같은 판단은 부위별 등급을 아무리 합쳐도 나오지 않는다 — 합계에는
비율이 없기 때문이다. 그래서 종합은 원본 두 장을 직접 본다.

━━ 그래도 F08 과 한 호출로 합치지 않는 이유 ━━

  * 보는 것이 다르다. F08 은 부위 하나하나의 윤곽을, F09 는 전체 형태를 본다.
    한 호출에 섞으면 모델이 둘 중 하나를 대충 한다 (긴 출력의 뒤쪽이 성기다)
  * F08 이 성공했는데 F09 만 실패해도 부위별 진단은 남는다
  * 종합 문구만 튜닝할 때 부위 진단을 다시 돌리지 않아도 된다

llm-strategy.md §F09 의 "Prompt Chaining" 이 이것이다.

⚠️ 이미지 순서는 vlm.diagnose_overall 이 넣는 순서와 반드시 같아야 한다.
   어긋나면 레퍼런스와 사용자가 뒤바뀌어 종합이 정반대로 나온다.

━━ 2026-08-20 전면 재구성 — 이 프롬프트에 예문·문형 골격이 없는 이유 ━━

세 번 실측했다. ① 완성 예문을 "복사 금지" 표시와 함께 뒀더니 서로 다른
사용자 출력에 글자 그대로 반복됐다. ② 예문을 지우고 «» 골격만 남겼더니
골격 문장("지금은 목표 체형과 차이가 뚜렷한 상태예요…그중에서도 «부위»의
차이가 가장 큽니다…")이 «» 자리만 바뀐 채 그대로 나갔다. ③ 금지 문구를
X 예시로 나열했더니 그 X 문장이 출력에 나왔다. 결론: **완성된 한국어 문장은
지시문보다 강하다.** 예문·골격·X 문장 나열·과거 사고의 출력 인용, 전부
모델이 재현할 원본이 된다. 그래서 이 프롬프트는 내용 요구사항만 말하고
문장은 전혀 보여주지 않는다. 사고 이력은 이 docstring 과 코드 주석에만 남긴다.

과거 규칙 누적의 부작용도 같이 정리했다 — 금지·예외가 겹겹이 쌓이면서
모델이 입력을 분석하는 대신 규칙 사이에서 안전한 상투구를 고르는 경향이
관측됐다(같은 사진 쌍에도 판단이 아니라 회피성 일반론). 하드 규칙(측정 금지·
종목 금지·코드 결정 존중 등)만 남기고 표현 관련 미세 규칙은 원칙으로 흡수했다.
"""

from typing import Any

SYSTEM_PROMPT = """당신은 목표 체형 사진과 현재 체형 사진을 나란히 놓고 전체를 비교하는 전문가입니다.

# 근거는 세 종류뿐이고, 각각 말할 수 있는 것이 다릅니다

모든 문장은 아래 셋 중 어느 근거에서 나왔는지 분명해야 합니다. 근거가 없는
문장은 쓰지 마세요 — 모른다고 두는 것이 지어내는 것보다 낫습니다.

1. **사진 두 장** — 목표 체형(레퍼런스)과 현재 체형(사용자).
   말할 수 있는 것: 실루엣, 윤곽, 두께·폭의 «보이는» 비율, 부위 사이의 비례,
   좌우의 외형 차이. 전부 «보입니다/나타납니다» 같은 관찰 어투로.
   말할 수 없는 것: 치수(cm 등 단위), 근육량, 체지방률, 근력, 골격 구조,
   건강 상태. 사진은 형태만 보여줍니다.

2. **부위별 진단 결과** — 앞 단계가 부위 하나씩 매긴 격차 등급.
   **격차의 «크기»는 이 등급과 점수가 확정합니다 — 사진 인상으로 뒤집지
   마세요.** 사진이 맡는 것은 그 격차가 «어떤 형태로» 나타나는지의 서술
   입니다. 등급과 사진 인상이 어긋나 보이면 형태 서술을 조심스럽게 하고
   confidence 를 낮추세요.

3. **인바디 실측** (주어졌을 때만) — 근육량·체지방 같은 정량 값.
   근육량·체지방을 입에 올릴 수 있는 유일한 근거입니다. 인바디가 없으면
   그 단어들을 쓸 수 없습니다 — 형태 어휘(두께·볼륨·윤곽)로만 말하세요.

🔴 **«근력»은 어느 근거로도 잴 수 없습니다.** 사진도 인바디도 근력을 재지
   않습니다. "근력 차이가 크다" 같은 판정 문장을 만들지 마세요. 규칙이 준
   개선 방향을 옮길 때의 "근력 운동을 우선하는 방향" 같은 훈련 방향 표현만
   허용됩니다 — 그건 측정 주장이 아니라 계획이기 때문입니다.

🔴 **인바디 수치는 숫자를 옮겨 적는 것이 아니라 해석하는 것입니다.**
   수치를 쓴다면 사진에서 본 것과 연결하세요 — 사진의 형태 차이를 실측이
   같은 방향으로 뒷받침하는지, 아니면 실측은 평균 수준인데 형태만 달라
   보이는지. 사진과 인바디가 어긋나면 억지로 한쪽 결론으로 몰지 말고
   보이는 것과 재어진 것을 각자의 자리에 두세요.
   ⚠️ 인바디의 '표준 대비 %'는 일반인 평균과의 비교입니다. 목표 체형과의
   거리가 아닙니다 — 두 잣대를 섞지 마세요.

# 판단 순서 — 부위를 이어 붙이지 말고 전체에서 시작하세요

부위별 결과를 요약하는 것은 당신의 일이 아닙니다. 카드에 없는 것, 즉
**전체**를 판단하세요. 순서는:

  1. 두 사진의 전체 실루엣에서 **가장 큰 차이 하나**를 찾는다
     — 비율·균형·윤곽의 관계로 (어깨-허리-골반 폭의 관계, 몸통 두께 대비
     팔 굵기, 상하체 볼륨 균형, 가슴~허리 윤곽의 굴곡 등에서 고르되
     이 목록에 없어도 사진에서 실제로 본 것이면 됩니다)
  2. 그 차이를 **만드는 주요 부위**를 짚는다 — 부위별 결과와 인바디로 확인
  3. 아래에 주어지는 규칙의 우선순위·개선 방향과 **연결**한다

같은 판단 하나가 summary → silhouette → key_differences 를 관통해야 합니다.
앞 문장에서 몸통을 말하다가 다음 문장에서 근거 없이 팔로 건너뛰면, 읽는
사람에게는 서로 다른 진단 셋을 이어 붙인 것으로 보입니다. 우선순위 1순위
부위가 전체 차이의 주인공과 다르면, 그 사이를 잇는 근거를 한 번은 쓰세요.

# 표현의 강도는 격차의 크기를 따라갑니다

부위 등급과 점수가 이미 주어집니다. 문장의 세기를 거기에 맞추세요.

- 등급이 대부분 NONE·SLIGHT 이고 점수가 높으면, 본론은 «이미 목표에
  가깝다»입니다. 우선순위 부위가 있어도 그것은 «그나마 남은 차이»이지
  «개선이 필요한 문제»가 아닙니다 — "가장 개선이 필요"류 표현을 쓰면
  높은 점수와 모순됩니다. 남은 차이의 정도를 낮춰 말하세요.
- 큰 격차(MODERATE·SIGNIFICANT)가 여럿이면 얼버무리지 말고 무엇이 어떻게
  다른지 구체적으로 쓰세요.
- 🔴 **등급이 NONE 인 부위를 «다르다»고 쓰지 마세요.** 사진에서 달라 보여도,
  그 부위는 이미 목표와 거의 같다고 판정됐습니다 — 확실치 않은 인상으로
  판정을 뒤집지 말고 그 부위는 언급을 줄이거나 가깝다고 쓰세요.

어느 경우든 어떤 사진에도 그대로 붙는 서술("차이가 있다/뚜렷하다",
"개선이 필요하다", "전반적으로 다르다")로 문장을 때우지 마세요 — 그런
문장이 나왔다면 사진을 안 본 것입니다.

# 문장은 매번 처음부터 새로 짜세요

이 프롬프트에는 따라 쓸 예문이 없습니다 — 일부러 두지 않았습니다. 정해진
문형·골격·서두도 없습니다. 위의 내용 요구만 지키고, 문장 구조는 이번 입력에
맞게 스스로 정하세요. 어딘가에서 본 듯한 익숙한 문장이 만들어지고 있다면
그 문장을 버리고 지금 두 사진에서 본 것으로 다시 쓰세요.

# 하드 규칙

- **점수·개선 우선순위·개선 방향은 코드가 이미 정했고 아래에 주어집니다.**
  우선순위는 당신이 정하지 않습니다 — 순서를 바꾸거나 다른 부위를 끼워 넣지
  말고, 그 결과를 사용자 말로 설명만 하세요. 점수와 다른 인상을 주는 문장
  (높은 점수인데 심각한 어조, 낮은 점수인데 거의 같다는 어조)도 금지입니다.
- **루틴은 «전신 + 약점 보강»입니다. 부위를 순서대로 하지 않습니다.**
  사용자는 다음 화면에서 전신 루틴을 받고, 격차가 큰 부위에만 세트가 더
  얹힙니다. "○○부터 시작", "이번 4주는 ○○만", "1~2주차엔 ○○" 전부 사실과
  다릅니다. 언제나 "전신을 하되 어디에 무게가 더 실리는가"로 말하세요.
  루틴은 아직 만들어지지 않았으므로 예고형으로 씁니다.
- **분석되지 않은 부위(아래 목록으로 주어짐)는 어느 필드에서도 언급 금지.**
  사진에 일부가 보여도 마찬가지입니다 — 분석이 안 됐다는 건 형태를 신뢰할
  만큼 못 봤다는 뜻입니다. 실루엣은 분석된 부위들 사이의 관계로만 쓰세요.
- **운동 종목·동작 계열 이름 금지.** 종목은 다음 화면의 루틴이 카탈로그에서
  고릅니다. 방향은 형태로만 말하세요(어느 부위에 무게를 두는가).
- 의학적 진단·질병·체중·감량·마름 언급 금지. 인바디 수치가 있어도 감량
  여부의 판단은 규칙이 이미 했고 그 결과만 주어집니다.
- 결과를 약속하지 마세요. 4주는 루틴 한 사이클이며 다른 기간으로 바꿔
  말하지 마세요.
- 몸 전체를 묶은 총평("전반적으로 부족")을 만들지 마세요 — 점수가 이미 그
  역할을 합니다.
- 레퍼런스와 똑같은 몸을 보장하는 표현 금지. 개선 가능한 차이를 기준으로.
- 읽는 사람은 운동을 처음 하는 본인입니다. "사용자의 ~" 같은 3인칭 말고
  부위를 주어로, 해부학 용어 대신 쉬운 말, 부위는 «왼쪽/오른쪽»으로,
  평가·비하 없이 개선 관점으로 쓰세요. 부위 이름은 주어진 한글 이름만
  쓰세요 — 모든 필드는 한국어 문장이며, Left_Upper_Arm 같은 영문 코드가
  출력에 나가면 화면에 그대로 노출됩니다.

# 출력 형식

JSON 하나만 반환합니다. `<>` 는 채워 넣을 자리 표시입니다 — 자리 표시나
이 명세의 문장을 출력에 복사하지 마세요.

{
  "user_profile": {
    "summary": "<현재 사진의 전체 프레임 특징 1~2문장, 80자 이내>",
    "characteristics": ["<관찰 항목 2~3개, 각 35자 이내>"]
  },
  "reference_profile": {
    "summary": "<레퍼런스의 전체 프레임 특징 1~2문장, 80자 이내>",
    "characteristics": ["<같은 기준으로 2~3개>"]
  },
  "summary": "<3문장, 전체 110자 이내 — 아래 요구사항>",
  "silhouette": "<전체 형태 비교 2문장 — 아래 요구사항>",
  "key_differences": ["<사진에서 직접 확인한 핵심 차이 1~2개, 각 40자 이내>"],
  "strengths": ["<NONE/SLIGHT 근거가 있는 부위의 좋은 점 — 없으면 빈 배열>"],
  "cautions": ["<주의할 점 — 없으면 빈 배열>"],
  "direction_summary": "<주어진 개선 방향을 사용자 말로 1문장, 55자 이내>",
  "strategy_focus": ["<주어진 모드·우선 부위로 설명하는 훈련 초점 1~2개, 각 40자>"],
  "next_cycle": "<4주 뒤 무엇이 달라지는지 1문장, 50자 이내>",
  "confidence": <0.0~1.0>
}

## 필드별 내용 요구사항 — 형식이 아니라 담을 내용입니다

- summary : 사용자가 가장 먼저 읽습니다. 정확히 3문장, 문장당 35자 안팎.
    1문장 — 판단 순서 1에서 찾은 **전체 차이**. 부위 이름이나 부위 사이의
            관계가 최소 하나 들어가야 합니다. 이 두 사진에서만 나올 수 있는
            내용이어야 합니다.
    2문장 — 남은 격차 중 가장 큰 곳. **그 크기는 점수 해석과 같은 세기로**
            말하세요 — 점수가 높으면 «그나마 남은 차이»이지 «필요한 개선»이
            아닙니다. 1문장과 같은 말의 반복이면 그 대신 왜 그곳이
            우선인지, 또는 그다음으로 먼 곳을 쓰세요.
    3문장 — 4주 루틴의 무게 배분 예고. 전신 전제를 반드시 담고, 지목하는
            부위는 주어진 우선순위의 첫 부위와 같아야 합니다.
    칭찬은 NONE/SLIGHT 등급 부위가 있을 때만 가능하고, 상대적으로 덜 나쁜
    부위를 좋다고 말하는 것은 거짓입니다.
- silhouette : 판단 순서 1·2를 사진 근거로 푼 **정확히 2문장.** 목표와 현재의
    비교는 **한 문장 안에** 나란히 담으세요 — 목표 문장 따로, 현재 문장
    따로 쪼개면 문장 수가 늘고 비교가 흩어집니다. 한쪽만 서술하면 비교가
    아니라 묘사입니다. 두 문장은 서로 다른 축을 다뤄야 하며, 상하체 축은
    다리가 실제로 분석된 경우에만 쓸 수 있습니다. «무엇을 어떻게 하는
    방향»은 여기서 한 번만 말합니다 — 부위 카드에는 방향이 없으므로
    여기가 유일한 자리입니다.
- key_differences : 사진에서 직접 확인했고 **부위 카드로는 안 보이는** 관계
    1~2개. 부위 하나의 등급을 옮겨 적는 곳이 아닙니다. 확인된 것이 하나면
    하나만 쓰세요.
- user_profile / reference_profile : 각각 그 사진 하나만 놓고 본 특징.
    비교문 금지(비교는 silhouette 의 일). 두 프로필은 같은 축을 봐야 서로
    견줄 수 있습니다. 골격이 아니라 «보이는 프레임 특징»입니다.
- direction_summary : 주어진 개선 방향의 «판정 + 근거»를 사용자 말로 옮긴
    1문장, 55자 이내. 방향을 새로 정하지 말고, 근거가 빠지면 summary 와
    같은 말이 되므로 반드시 근거를 녹이세요. 세트 수·종목·일수 금지.
- strategy_focus : 주어진 모드와 우선 부위로 설명하는 훈련 초점. 종목 금지.
- next_cycle : 우리 루틴에 단계·페이즈는 없습니다. 말할 수 있는 것은 4주 뒤
    재측정으로 방향을 다시 판정한다는 사실뿐입니다. 로드맵을 만들지 마세요.
- strengths : NONE/SLIGHT 근거가 있는 부위만, **한국어 문장으로** (부위
    코드가 아니라 그 부위의 무엇이 좋은지). 없으면 빈 배열이 정답입니다.
- cautions : 좌우 불균형, 판단 불가 부위가 많음 등 — 한국어 문장으로.
    없으면 빈 배열.
- confidence : 이 종합을 얼마나 믿을 수 있는가 (유사도 점수와 무관합니다).
    0.8~1.0 전신이 뚜렷하고 부위 진단과 일치 / 0.5~0.8 일부 가림 /
    0.2~0.5 판단 불가가 많거나 흐림 / 0.2 미만 사실상 못 봄 — 이때는
    silhouette 에도 그렇게 쓰세요. 판단 불가 부위가 절반을 넘으면 0.5
    아래여야 합니다.

⚠️ priority_parts / strengths / cautions / key_differences / strategy_focus 는
   전부 배열입니다. 문자열 하나로 이어붙이지 마세요."""


def _part_line(p: dict[str, Any]) -> str:
    # ⚠️ blocked_reason 이 있어도 gap_level 이 있으면 판단 불가가 아니다.
    #    옷에 가렸지만 인바디로 등급을 매긴 경우가 이에 해당한다. 여기서 뭉뚱그리면
    #    인바디로 건진 부위가 다시 "모름"으로 떨어져 점수에서 빠진다.
    if not p.get("gap_level"):
        reason = p.get("blocked_reason") or "사유 미기재"
        return f"- {p['class_name']}: 판단 불가 — {reason}"

    bits = [f"격차 {p['gap_level']}"]
    if p.get("priority") is not None:
        bits.append(f"우선순위 {p['priority']}")
    if p.get("confidence"):
        bits.append(f"확신도 {p['confidence']}")
    if p.get("blocked_reason"):
        # 시각은 막혔지만 인바디로 등급이 나온 부위 — 근거가 다르다는 걸 알려준다.
        bits.append("시각 확인 불가 · 인바디 기준")

    line = f"- {p['class_name']} ({' · '.join(bits)})"
    if p.get("assessment"):
        line += f"\n    {p['assessment']}"
    for d in p.get("differences") or []:
        line += f"\n    · {d}"
    return line


def _inbody_block(inbody: dict[str, Any] | None) -> str:
    if not inbody:
        return (
            "인바디 결과 없음 — 근육량·체지방이라는 단어를 쓸 근거가 없습니다. "
            "형태 어휘로만 종합하세요."
        )

    body = inbody.get("body") or {}
    bits = []
    for label, key, unit in (
        ("체중", "weight", "kg"),
        ("골격근량", "skeletal_muscle_mass", "kg"),
        ("체지방률", "body_fat_percentage", "%"),
        ("BMI", "bmi", ""),
    ):
        if body.get(key) is not None:
            bits.append(f"{label} {body[key]}{unit}")

    lines = [" · ".join(bits)] if bits else []
    has_pct = False
    for segment, s in (inbody.get("segments") or {}).items():
        if s.get("lean_mass") is not None:
            extra = (
                f" (표준 대비 {s['lean_percentage']}%)"
                if s.get("lean_percentage") is not None
                else ""
            )
            has_pct = has_pct or s.get("lean_percentage") is not None
            lines.append(f"- {segment}: 제지방 {s['lean_mass']}kg{extra}")

    lines.append(
        "※ 수치를 문장에 쓸 때는 숫자만 옮기지 말고 사진에서 본 형태와 연결해"
        " 해석하세요 (시스템 규칙 참고)."
    )
    if has_pct:
        lines.append(
            "※ 표준 대비 %는 **일반인 평균과의 비교**입니다. "
            "목표 체형과의 거리처럼 인용하지 마세요."
        )
    return "\n".join(lines) if lines else "인바디 수치 없음"


#: 부위 비중 표의 그룹 전체. ⚠️ handlers/vlm.py `_SHARE_GROUPS` 의 키와 같아야 한다 —
#: 표에 빠진 그룹을 찾아 "언급 금지"를 걸기 위한 기준 목록이다.
_SHARE_GROUP_LABELS = ("몸통", "팔", "하체")

#: 격차 등급 서열 (동률 판정용). scoring._GAP_ORDINAL 과 같은 값 —
#: import 하면 prompts→services 역방향 의존이 생겨 여기 복사해 둔다.
_GAP_ORDINAL = {"NONE": 0, "SLIGHT": 1, "MODERATE": 2, "SIGNIFICANT": 3}


def _tie_note(parts: list[dict[str, Any]], priority_parts: list[str]) -> list[str]:
    """우선순위 1순위와 같은 등급(동률)인 부위가 더 있으면 그 사실을 명시한다.

    scoring.rank_priority 는 동률을 이름순으로 줄 세울 뿐이므로, 목록만 주면
    모델이 [0]번 부위를 "가장 크다"로 서술한다. 동률이면 함께 짚게 한다.
    """
    by_name = {p.get("class_name"): p for p in parts}
    top = by_name.get(priority_parts[0]) if priority_parts else None
    if not top or top.get("gap_level") not in _GAP_ORDINAL:
        return []
    top_gap = top["gap_level"]
    tied = [
        name
        for name in priority_parts
        if (by_name.get(name) or {}).get("gap_level") == top_gap
    ]
    if len(tied) < 2:
        return []
    return [
        f"⚠️ 격차 최상위 동률: {', '.join(tied)} — 전부 같은 등급({top_gap})입니다.",
        "   목록의 순서는 이름순일 뿐이니, 한 부위만 «가장 크다»고 쓰지 말고",
        "   동률인 부위들을 함께 짚으세요.",
    ]


def build_overall_prompt(
    parts: list[dict[str, Any]],
    blocked: list[dict[str, Any]],
    failed: list[str],
    inbody: dict[str, Any] | None,
    score: int | None = None,
    excluded: list[str] | None = None,
    has_images: bool = True,
    priority_parts: list[str] | None = None,
    direction: dict[str, Any] | None = None,
    cut_notice: str | None = None,
    shares: dict[str, Any] | None = None,
) -> str:
    """F09 사용자 메시지.

    ⚠️ 이미지는 이 함수가 만들지 않는다. vlm.diagnose_overall 이 이 텍스트 **앞에**
       레퍼런스 원본 → 사용자 원본 순서로 붙인다. 아래 §사진 안내가 그 순서를
       설명하므로, 순서를 바꾸려면 두 곳을 같이 고쳐야 한다.

    Args:
        parts:   판단에 성공한 부위 진단 (gap_level 이 있는 것)
        blocked: 판단 불가로 처리된 부위 (gap_level 이 null)
        failed:  응답 자체가 없거나 형식이 깨진 부위의 class_name
        excluded: **분석 자체가 안 된** 비교 대상 부위 (미검출·교집합 탈락).
                 이 목록이 없으면 LLM 이 본 적도 없는 부위를 언급한다 —
                 실연동에서 실제로 났던 사고다 (A 발견. 하체 미검출 세션에서
                 silhouette 과 key_differences 가 하체를 서로 반대 방향으로
                 언급했다 — 둘 다 근거 0).
        score:   scoring.compute_similarity() 가 계산한 유사도 점수.
                 LLM 은 이 값을 **받아서 쓸 뿐** 만들지 않는다 (score_source=RULE).
        has_images: 사진 두 장이 함께 전송되는가. False 면 "사진 없음"을 명시해
                 모델이 보지 않은 형태를 지어내지 않게 한다.
        priority_parts: **규칙이 이미 정한** 개선 우선순위 (scoring.rank_priority).
                 LLM 은 이 순서를 바꾸지 않고 설명만 한다. 안 넘기면 그 절을 생략한다.
        direction: **규칙이 이미 정한** 개선 방향 (scoring.decide_direction).
                 mode / mode_reason / priority / reason 을 담는다. LLM 은 설명만 한다.
        cut_notice: CUT 모드일 때의 전략 설명 (routine_templates.CUT_NOTICE).
                 ⚠️ LLM 이 감량 전략을 새로 만들지 않게 **완성된 문장을 준다.**
        shares: 상체·하체 면적 비중 (segmap.compare_parts 집계).
                 ⚠️ 이게 없으면 모델이 실루엣의 **방향을 뒤집는다** — 실측
                    (2026-08-18): 몸통이 +13%, 하체가 −2% 인데 하체 비중이 더
                    높다고 썼다. 눈으로만 보면 어느 쪽이 더 큰지 헷갈리므로
                    숫자로 못 박는다.
    """
    sections = [
        *(
            [
                "# 사진",
                "",
                "위에 이미지 두 장이 순서대로 주어졌습니다.",
                "",
                "  1번째 — 목표 체형(레퍼런스) 원본",
                "  2번째 — 현재 체형(사용자) 원본",
                "",
                "두 장을 직접 비교해 silhouette · key_differences 를 쓰세요.",
                "부위별 등급은 아래 결과를 참고하되, 사진과 충돌하면 사진을 우선합니다.",
                "",
            ]
            if has_images
            # ⚠️ 사진이 못 붙은 경우. 있다고 말하면 모델이 본 적 없는 것을 지어낸다.
            else [
                "# 사진",
                "",
                "이번에는 사진이 제공되지 않았습니다. 아래 부위별 진단 결과만으로",
                "판단하세요. silhouette 과 key_differences 에는 사진에서 확인한 것처럼",
                "쓰지 말고, 확인할 수 없었다고 밝히거나 비워 두세요.",
                "confidence 는 0.3 을 넘기지 마세요.",
                "",
            ]
        ),
        *(
            [
                "# 개선 우선순위 (규칙이 이미 정했습니다)",
                "",
                "  " + " → ".join(priority_parts),
                "",
                "이 순서를 바꾸거나 다른 부위를 끼워 넣지 마세요. summary 에서 이 결과를",
                "설명하되, 어느 부위를 먼저 할지 **당신이 새로 고르지 않습니다.**",
                # ⚠️ 동률 표시도 코드가 한다. 우선순위 목록만 주면 모델이 [0]번을
                #    "가장 크다"로 옮긴다 — 서로 다른 부위들이 같은 등급으로 묶였을
                #    때는 순서가 이름순일 뿐인데, 한 부위만 콕 집으면 나머지 동률
                #    부위가 밀린 것처럼 읽힌다 (FE 헤드라인과 같은 문제·같은 해법).
                *_tie_note(parts, priority_parts),
                "",
            ]
            if priority_parts
            else []
        ),
        *(
            [
                "# 개선 방향 (규칙이 이미 정했습니다)",
                "",
                f"  판정      : {direction.get('priority')}",
                f"  근거      : {direction.get('reason')}",
                f"  운동 모드 : {direction.get('mode')}  (판정 근거: {direction.get('mode_basis')})",
                *(
                    [f"  모드 설명 : {direction['mode_reason']}"]
                    if direction.get("mode_reason")
                    else []
                ),
                *(
                    [
                        "",
                        "  감량 모드 전략 (이미 정해진 내용 — 새로 만들지 마세요):",
                        f"  {cut_notice}",
                    ]
                    if cut_notice
                    else []
                ),
                "",
                "🔴 이 판정을 바꾸지 마세요. direction_summary·strategy_focus 는",
                "   위 내용을 **사용자 말로 옮기는 것**이지 새로 정하는 게 아닙니다.",
                "⚠️ 체지방·감량 판단은 인바디에서 나왔습니다. 사진으로 판단하지 마세요.",
                "",
            ]
            if direction
            else []
        ),
        *(
            [
                "# 두 사진의 부위 비중 (계산 완료 — 이 방향을 뒤집지 마세요)",
                "",
                "  같은 사람 안에서 «비교 대상 부위 전체» 대비 각 부위가 차지하는 면적 비율입니다.",
                "  촬영 거리·해상도가 상쇄되므로 두 사진을 그대로 견줄 수 있습니다.",
                "",
                *[
                    f"  {label:<10} 레퍼런스 {v['reference']:.3f} → 현재 {v['user']:.3f}"
                    f"  ({v['delta']:+.0f}%{' — 현재가 더 큼' if v['delta'] > 3 else (' — 현재가 더 작음' if v['delta'] < -3 else ' — 비슷함')})"
                    for label, v in shares.items()
                ],
                "",
                "🔴 silhouette·key_differences 는 **이 수치와 같은 방향**이어야 합니다.",
                "   숫자가 «현재가 더 큼» 이라고 말하는 부위를 «작다»고 쓰면 안 됩니다.",
                "",
                # ⚠️ 표에 없는 그룹 = 그쪽 부위가 비교 대상에 하나도 못 들었다는 뜻.
                #    실측(2026-08-18) — 다리 4부위가 전부 미검출(프레임 잘림)인 세션에서
                #    모델이 사진에 조금 보이는 잘린 다리를 근거로 상하체 볼륨 비교를
                #    summary·key_differences·프로필에 걸쳐 썼다. 잘린 부위는 작아
                #    «보이는» 것이지 작은 게 아니다.
                *(
                    [
                        f"🔴 위 표에 없는 그룹({', '.join(g for g in _SHARE_GROUP_LABELS if g not in shares)})은",
                        "   그쪽 부위가 비교 대상에 하나도 들지 못했다는 뜻입니다 (사진에서 잘렸거나",
                        "   검출되지 않음). 사진에 일부가 보여도 프레임에 잘린 모습이라 크기·비중을",
                        "   판단할 수 없습니다. silhouette · key_differences · summary · 프로필",
                        "   어디에서도 그 그룹의 크기·비중·볼륨을 언급하지 마세요.",
                        "",
                    ]
                    if any(g not in shares for g in _SHARE_GROUP_LABELS)
                    else []
                ),
            ]
            if shares
            else []
        ),
        "# 부위별 진단 결과",
        "",
        "\n".join(_part_line(p) for p in parts) if parts else "(판단된 부위 없음)",
    ]

    if score is not None:
        # ⚠️ 해석(구간 라벨)까지 코드가 정한다. 숫자만 주면 모델이 92점을 보고도
        #    "개선이 필요"라고 쓴다 — 어조의 기준선을 결정론적으로 못 박는다.
        band = (
            "목표에 거의 근접한 상태 — 남은 차이는 «미세 보완»으로 서술"
            if score >= 90
            else "목표에 가까운 편 — 남은 차이를 문제로 부풀리지 말 것"
            if score >= 70
            else "차이가 분명한 상태 — 무엇이 다른지 구체적으로"
            if score >= 40
            else "차이가 큰 상태 — 얼버무리지 말고 구체적으로, 단 개선 폭이 크다는 관점으로"
        )
        sections += [
            "",
            f"# 유사도 점수 (계산 완료): {score}점 — {band}",
            "",
            "※ 부위별 격차 등급을 규칙으로 합산한 값입니다. 점수를 새로 만들지 말고,",
            "  summary·silhouette 의 어조를 위 해석에 맞추세요.",
        ]

    if blocked:
        sections += [
            "",
            "## 판단 불가 부위",
            "",
            "\n".join(_part_line(p) for p in blocked),
            "",
            "※ 이 부위들은 점수에 반영하지 마세요. 다만 판단 불가가 많다면 "
            "cautions 에 '일부 부위는 복장·각도로 확인이 어려웠다'는 취지를 넣으세요.",
        ]

    if failed:
        sections += [
            "",
            f"## 진단 실패 부위: {', '.join(failed)}",
            "",
            "※ 기술적 실패입니다. 점수에 반영하지 말고 언급도 하지 마세요.",
        ]

    if excluded:
        # ⚠️ 한글 이름 목록을 코드가 뽑아서 준다. "괄호 안의 한글 이름을 쓰라"는
        #    지시만으로는 영문 코드가 cautions 에 그대로 나간 실측이 있다 —
        #    모델에게 추출을 시키지 말고 완성된 데이터를 준다.
        ko_names = [
            name[name.index("(") + 1 : name.rindex(")")] if "(" in name and ")" in name else name
            for name in excluded
        ]
        sections += [
            "",
            f"## 분석되지 않은 부위: {', '.join(excluded)}",
            "",
            "※ 사진에서 검출되지 않아 **아무 정보가 없는** 부위입니다.",
            "🔴 **silhouette · key_differences · summary · strengths 어디에도",
            "   이 부위를 넣지 마세요.** 사진에 그 부위가 보이더라도 마찬가지입니다 —",
            "   분석이 안 됐다는 건 형태를 신뢰할 만큼 못 봤다는 뜻입니다.",
            "🔴 실루엣은 **분석된 부위들 사이의 관계**로만 쓰세요.",
            "   위아래(상하체) 축이 막히면 좌우 폭·두께·윤곽 축으로 옮기면 됩니다.",
            "  cautions 에 언급한다면 **전부 묶어 한 문장**으로 쓰세요.",
            "  부위마다 한 문장씩 반복하면 주의 목록이 같은 말로 도배됩니다.",
            f"  그 문장에 쓸 이름은 이것뿐입니다 (한글만): {', '.join(ko_names)}",
            "  영문 부위 코드는 화면에 그대로 노출되므로 절대 출력하지 마세요.",
        ]

    sections += [
        "",
        "# 인바디",
        "",
        _inbody_block(inbody),
        "",
        "# 요청",
        "",
        "위 결과를 종합해 최종 진단을 JSON 으로 반환하세요.",
    ]
    return "\n".join(sections)
