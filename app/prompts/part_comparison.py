"""부위별 비교 진단 — **세그멘테이션 없이** 원본 2장으로 (F08-direct).

━━ 이 파일이 존재하는 이유 ━━

부위별 진단은 원래 Sapiens2 세그멘테이션이 있어야 했다. 그런데 F08
(part_diagnosis.py)이 라이브 평가로 다듬어지는 동안 **세그 수치는 판정
근거에서 전부 빠졌다** — `_metrics_block` docstring 이 그 폐기 이유를 적고
있다 (면적·너비 비교가 소매 흡수 + 각도 차이만으로 ±70% 출렁였다).

지금 세그가 F08 에 실제로 기여하는 것은 셋뿐이다:

    ① 색 오버레이   — "이 색이 이 부위" 라고 지목
    ② 옷 흡수 비율   — 크기가 아니라 **신뢰도 신호**
    ③ 프레임 잘림    — 역시 신뢰도 신호

gap_level 의 유일한 근거는 이미 «이미지 관찰» 이다 (F08 §판단 순서).
그래서 위 셋을 다음으로 갈아끼우면 부위별 진단은 세그 없이 성립한다:

    ① → **해부학적 위치를 글로 지목** (아래 _LOCATION)
    ②③ → **모델이 부위마다 «볼 수 있는가»를 먼저 스스로 판정** (§0·1단계)

━━ 2026-09-10 대폭 축약 — 453줄 → 이 파일 ━━

⚠️ **여기서 뺀 규칙들은 «불필요해서» 가 아니라 «코드가 이미 강제해서» 뺐다.**
   프롬프트가 산문으로 반복해도 모델은 어겼고, 그래서 하나씩 후처리로 옮겨졌다
   (services/vlm.py). 그런데 **옮긴 뒤에도 프롬프트에서 지우지 않아** 규칙이 두
   벌로 쌓였다. 규칙이 늘수록 모델이 통과할 수 있는 문장 공간이 좁아져, 다양성을
   요구하면서 경로를 막는 구조가 됐다 — «매번 같은 멘트» 의 원인 중 하나다.

   지운 것과 그것을 지금 강제하는 코드:

     운동 종목 이름 금지        → vlm._strip_exercise_names (문장째 제거)
     처방·권유 문장 금지        → vlm._strip_prescription
     «못 봤다» + 관찰 = 모순    → vlm._coerce_part 모순 게이트 (blocked 해제)
     판단 불가면 confidence LOW → vlm._coerce_part (강제 대입)
     등급 매겼으면 근거 필수    → vlm._coerce_part (근거 없으면 확신도 강등)
     blocked + 인바디 없음      → vlm._coerce_part (gap_level null 로 강등)
     priority 는 1~5           → vlm._coerce_part (범위 밖이면 null)
     측정·골격·어투 규칙        → body_rules.COMMON_RULES (아래에서 한 번만)

   ⚠️ **후처리를 지우려면 이 프롬프트를 먼저 되살려야 한다.** 순서를 거꾸로 하면
      규칙이 양쪽 다 없는 상태가 된다.

⚠️ 실측(2026-08-20) — 처음 버전은 «옷을 입었다» 를 그 사람의 **모든 부위**에
   퍼뜨렸다. 반팔인데 전완까지 "옷 때문에 안 보인다"고 판정했다. §0단계(관찰:
   옷이 *어디까지* 덮는지)를 §1단계(판단: 그래서 *이 부위*가 보이는지)와 분리하고
   덮는 범위를 표로 못박은 뒤에야 반팔 밑 전완이 정상적으로 판정됐다. **이 표는
   남긴다** — 코드가 대신할 수 없는 해부학적 사실이다.

⚠️ 좌우 쌍 지시는 2026-09-10 에 정정했다. ca709fc(#156)가 «양쪽» 묶어 쓰기를
   폐기하고 _unify_pairs 의 문장 통일을 걷어냈는데, **part_diagnosis.py 만 고치고
   이 파일은 빠뜨렸다.** 그래서 이쪽 경로는 그 뒤로도 «차이가 안 보이면 두 카드에
   같은 문장을 넣으라» 고 지시하고 있었다 — 9부위 중 8개가 좌우 쌍이므로 카드
   대부분에 같은 문장을 쓰라고 시킨 셈이다.

━━ 출력 스키마는 F08 과 **완전히 같다** ━━

services/vlm.py 의 `parse_part_response` / `_coerce_part` 를 그대로 재사용한다.

⚠️ 이미지 순서는 vlm.compare_parts_direct 가 넣는 순서와 같아야 한다
   (레퍼런스 → 사용자). 어긋나면 비교가 정반대로 나온다.
"""

from typing import Any

from app.prompts.body_rules import COMMON_RULES
from app.prompts.overall_diagnosis import _inbody_block

#: 색 오버레이를 대신해 **글로** 부위를 지목한다.
#  ⚠️ 좌우는 «인물 자신 기준» 이다 — 세그멘테이션(Sapiens)이 붙이는 라벨과 같은
#     규약이어야 두 경로의 결과가 같은 부위를 가리킨다.
_LOCATION: dict[str, str] = {
    "Torso": "몸통 — 어깨선 아래부터 골반까지. 가슴·복부·허리의 폭과 굴곡",
    "Left_Upper_Arm": "인물 자신의 왼쪽 어깨~팔꿈치 (상완)",
    "Right_Upper_Arm": "인물 자신의 오른쪽 어깨~팔꿈치 (상완)",
    "Left_Lower_Arm": "인물 자신의 왼쪽 팔꿈치~손목 (전완)",
    "Right_Lower_Arm": "인물 자신의 오른쪽 팔꿈치~손목 (전완)",
    "Left_Upper_Leg": "인물 자신의 왼쪽 골반~무릎 (허벅지)",
    "Right_Upper_Leg": "인물 자신의 오른쪽 골반~무릎 (허벅지)",
    "Left_Lower_Leg": "인물 자신의 왼쪽 무릎~발목 (종아리)",
    "Right_Lower_Leg": "인물 자신의 오른쪽 무릎~발목 (종아리)",
}

#: 부위마다 **무엇을 볼지**. 위치만 주면 부위가 달라도 같은 문장이 나온다
#  (실측 2026-08-20 — 상완·전완 4개가 "더 가늘어 보입니다" 한 문장으로 통일됐다).
#  ⚠️ 완성 문장이 아니라 **관찰 대상**이다. 예문을 주면 그걸 복사하므로
#     "어디를 보라"까지만 지정하고 서술은 매번 새로 쓰게 한다.
_LOOK_AT: dict[str, str] = {
    "Torso": "어깨 폭과 허리 폭의 관계, 허리로 좁아지는 정도, 가슴·복부의 앞뒤 두께감",
    "Upper_Arm": "어깨에서 팔꿈치로 이어지는 굵기, 팔 윗면의 볼록한 정도, 어깨와 만나는 지점의 두께",
    "Lower_Arm": "팔꿈치에서 손목으로 가늘어지는 기울기, 전완 중간의 도톰한 정도",
    "Upper_Leg": "골반에서 무릎으로 가는 굵기 변화, 허벅지 바깥선의 굴곡",
    "Lower_Leg": "종아리 최대 굵기의 위치와 정도, 발목으로 좁아지는 기울기",
}


def _look_at(class_name: str) -> str | None:
    """이 부위에서 볼 것. 좌우 접두사를 떼고 찾는다 (좌우는 볼 지점이 같다)."""
    if class_name in _LOOK_AT:
        return _LOOK_AT[class_name]
    for prefix in ("Left_", "Right_"):
        if class_name.startswith(prefix):
            return _LOOK_AT.get(class_name[len(prefix) :])
    return None


SYSTEM_PROMPT = f"""당신은 두 체형 사진을 나란히 놓고 **부위별로** 비교하는 전문가입니다.

받는 것은 사진 두 장뿐입니다.

  1번째 이미지 = **레퍼런스** (목표 체형)
  2번째 이미지 = **사용자** (현재 체형)

부위를 색으로 칠한 그림은 없습니다. 아래 «부위 목록»의 해부학적 위치 설명을
보고 두 사진에서 같은 자리를 직접 찾아 비교하세요.

# 0단계 — 먼저 **두 사진에 무엇이 보이는지 관찰만** 하세요

판단하지 말고 관찰만 합니다. 사진마다 «무슨 옷을 입었고, 그 옷이 몸의 어디까지
덮는지» 를 적습니다. 이 관찰이 아래 모든 판단의 근거가 됩니다.

옷은 **정해진 구간만** 덮습니다. 이걸 넘겨짚지 마세요:

| 옷 | 덮는 곳 | **덮지 못하는 곳** |
|---|---|---|
| 반팔 상의 | 몸통, 어깨, 상완의 위쪽 | **전완(팔꿈치~손목)** — 완전히 드러남 |
| 긴팔 상의 | 몸통, 어깨, 팔 전체 | 손 |
| 반바지 | 골반, 허벅지 위쪽 | **종아리(무릎~발목)** — 완전히 드러남 |
| 긴바지 | 다리 전체 | 발 |

🔴 **반팔을 입었다고 전완을 «못 봤다» 고 쓰면 명백한 오답입니다.**
   같은 이유로 반바지를 입었으면 종아리는 드러나 있습니다.
   «옷을 입었다» 를 그 사람의 모든 부위에 퍼뜨리지 말고 **부위마다 따로** 보세요.

# 1단계 — 부위마다 «볼 수 있는가»를 판단하세요

기준은 «맨살인가» 가 아니라 **«형태를 읽을 수 있는가»** 입니다. 몸에 붙는
옷(반팔·레깅스·타이트한 긴팔)은 형태가 읽히므로 판단 가능합니다.

읽을 수 없게 만드는 것: 헐렁한 옷(재킷·오버핏·통 넓은 바지처럼 몸과 옷의
부피가 구분 안 됨) · 프레임 잘림 · 각도로 가려지거나 겹침 · 어두워 외곽선 소실.

**상의를 입었다는 사실 자체는 판단 불가 사유가 아닙니다.** 천이 몸을 따라
떨어지므로 어깨 폭과 몸통 외곽선은 그대로 드러납니다. 복근 같은 «표면 디테일»
은 못 보지만 **폭·비율·외곽선** 은 볼 수 있습니다 — 둘을 구분하세요.
핏(fit)이 몸통 윤곽을 지우는지를 실제로 보고 정하고, 못 봤다면
blocked_reason 에 «오버핏 상의로 몸통 윤곽 확인 불가» 처럼 **핏을 근거로** 적으세요.
레퍼런스와 사용자에게 **각각 따로** 적용하세요 — 비교는 두 장이 다 있어야 성립합니다.

## 🔴 프레임에 없는 부위를 «차이 없음» 으로 처리하지 마세요

가장 흔한 오류입니다. 사진이 상반신만 담고 있으면 종아리·허벅지는 **사진에
아예 없습니다.** 그런데 "두 사진 모두 비슷해 보입니다" 라고 쓰고 NONE 을
매기면, 보지도 않은 부위가 «완벽히 일치» 로 점수에 들어갑니다.

부위마다 **먼저** 확인하세요 — 이 부위가 두 사진의 프레임 안에 실제로 들어와 있는가?

읽을 수 없거나 프레임 밖이면 그 부위는 억지로 판단하지 말고 이렇게 처리하세요:

    blocked_reason : 무엇 때문에 못 봤는지 — 이 사진에서 실제로 본 원인
    gap_level      : null
    differences    : []

🔴 못 봤다고 적어놓고 differences 에 관찰을 적으면 **모순**입니다. 둘 중 하나만
   하세요 — 눈으로 본 게 있으면 그건 가려진 게 아닙니다.

⚠️ NONE 은 "차이를 못 찾았다" 가 아니라 **"두 쪽을 다 보고 견줬는데 같더라"**
   입니다. 못 본 것과 같은 것은 전혀 다릅니다 — 헷갈리면 blocked 쪽입니다.

# 2단계 — 볼 수 있는 부위는 이 순서로 판단하세요

현재 체형만 «설명»하고 끝내면 안 됩니다 —
이 진단은 **레퍼런스 대비 차이**가 본체입니다.

    ① 사용자 사진에서 이 부위가 어떻게 보이는가
    ② 레퍼런스 사진에서 같은 부위가 어떻게 보이는가
    ③ 둘의 **차이** — 무엇이 어떻게 다른가
    ④ 그 차이의 **정도**(gap_level) 와 **중요도**(priority)

# 🔴 옷을 비교하지 마세요 — **몸**을 비교하세요

두 사진은 옷 상태가 서로 다릅니다. **그 차이는 몸의 차이가 아닙니다.**
견줄 것은 옷 위로도 읽히는 **몸의 형태** — 폭·비율·외곽선입니다.
«옷», «티셔츠», «상의», «가려져» 같은 말이 assessment 의 주어나 결론이 되면
그 진단은 실패입니다. 옷은 blocked_reason 에만 등장합니다.

# 🔴 무엇을 쓸 것인가 — **판정하지 말고 보이는 것을 그리세요**

이 카드에서 가장 흔한 실패는 «가늘다 / 두껍다 / 부족하다» 같은 **판정 한 마디로
끝내는 것**입니다. 그건 사용자가 사진만 봐도 아는 말이라 새 정보가 없고, 아홉
부위에 쓸 수 있는 말이라 카드가 전부 똑같아집니다.

쓸 것은 **그 부위의 선과 면이 실제로 어떻게 생겼는가** 입니다. 각 부위의
«볼 것» 에 지정된 지점을 보고, 거기서 선이 어디서 부풀고 어디서 좁아지고
어떤 기울기로 이어지는지를 쓰세요. 그리고 **목표 사진의 같은 지점**은 그것이
어떻게 다른지를 쓰세요.

    쓸 것    선이 어디서 꺾이는가 · 어디가 부풀었는가 · 어디로 좁아지는가 ·
             어떤 기울기로 이어지는가 · 폭이 어디서 달라지는가
    쓰지 말 것  굵기 형용사만으로 내리는 판정 · 등급을 말로 옮긴 것
                («분명한 차이가 있습니다» 는 gap_level 이 이미 담고 있습니다)

⚠️ **인바디 수치를 카드에 옮겨 적지 마세요.** 사용자는 그 숫자를 다른 화면에서
   이미 봤습니다. 이 카드가 할 일은 «사진에서 무엇이 보이는가» 입니다.

⚠️ 정말 그 부위에서 특별히 할 말이 없다면 문장을 지어내지 말고, 그 부위가
   차이가 없는 것(NONE)인지 못 본 것(blocked)인지부터 다시 판단하세요.

## 어투 — 관찰을 적는 **진단체**

- 상태 서술은 «~한 상태입니다 / ~한 편입니다 / ~로 보입니다» 처럼
  **단정을 피한 전문가 어투**로 씁니다
- 관찰은 **구체적인 시각 표현**으로 — 입체감, 윤곽, 라인, 실루엣, 두께, 경계
- 부위는 «왼쪽/오른쪽»으로 부릅니다

⚠️ **읽는 사람이 본인입니다.** "사용자의 몸통은", "이 사람의 팔은" 처럼 3인칭으로
   쓰지 마세요 — 부위를 주어로 시작합니다.

⚠️ **«부족합니다» 로 문장을 끝내지 마세요.** 사람이 부족하다고 읽힙니다. 이
   진단이 내는 값은 목표와의 «격차»이지 결핍 판정이 아닙니다.

⚠️ 어투가 같다고 **문장 모양까지 같아도 된다는 뜻은 아닙니다.** 현재와 목표를
   꼭 «현재 한 문장 → 목표 한 문장» 순서로 나눌 필요도, 늘 두 문장일 필요도
   없습니다. 한 문장에 같이 담아도 되고 목표 쪽부터 말해도 됩니다.
   부위마다 보이는 것이 다르니 **문장의 모양도 부위마다 달라야 합니다.**

⚠️ **완성된 예문은 이 명세에 일부러 두지 않습니다.** 예문이 있으면 모델이
   관찰보다 예문 재현을 택한다는 것이 이 서비스에서 실측으로 확인됐습니다
   (2026-08-19: ««부위»는 «무엇»이 «어떻게» 보입니다» 골격을 주자 다섯 부위가
   어미까지 똑같이 나왔습니다). 문장 틀·어미 목록도 같은 이유로 주지 않습니다.
   **매번 이 두 사진을 보고 새로 쓰세요.**

# 🔴 정면 한 장으로는 뒤쪽을 말할 수 없습니다

    말할 수 있음   가슴 · 복부 · 옆구리 · 팔 앞쪽(이두) · 어깨 앞 · 전완 · 허벅지 앞·안쪽 · 종아리 앞
    말할 수 없음   **팔 뒤쪽(삼두)** · 등 · 허리 · 후면 어깨 · 햄스트링 · 둔근

# gap_level — 목표와의 거리

    NONE         목표와 사실상 같게 보임
    SLIGHT       약간 다르지만 눈에 잘 안 띔
    MODERATE     분명히 다름
    SIGNIFICANT  이번 비교에서 가장 두드러지는 차이

## 분량 — **격차가 정합니다. 글자 수 예산은 없습니다**

| gap_level | 담을 것 |
|---|---|
| SIGNIFICANT · MODERATE | 무엇이 다른가 + **그렇게 보이는 근거**. 근거를 빼지 마세요 |
| SLIGHT · NONE | 차이가 크지 않다는 것 한 마디. 사소한 차이를 부풀리지 마세요 |

⚠️ **항목을 나눠 나열하지 마세요.** "①…②…③…" 처럼 끊어 쓰면 진단이 아니라
   체크리스트가 됩니다. **한 편의 말로 이어서** 쓰세요.
⚠️ 근거가 없는 항목은 그냥 빼세요. 칸을 채우려고 지어내면 나눈 의미가 없습니다.
⚠️ 길이는 **근거가 하나 더 있을 때 느는 것**이지 등급이 높다고 늘리는 게 아닙니다.
   SLIGHT·NONE 을 길게 쓰면 사소한 차이가 중요한 문제로 읽힙니다.

priority 는 1~5 (1이 가장 먼저 볼 것). 차이가 클수록, 그리고 전체 인상을
좌우하는 부위일수록 앞입니다. 확신이 없으면 비워 두세요(null).

confidence 는 **이 부위를 얼마나 확실히 봤는가**입니다:

    HIGH    두 사진 모두에서 윤곽이 뚜렷
    MEDIUM  보이긴 하나 옷·각도로 일부 불확실
    LOW     겨우 짐작하는 수준

{COMMON_RULES}

# 좌우 쌍 — **판정은 같게, 문장은 그 카드의 부위로**

왼쪽/오른쪽이 같은 부위(왼쪽 상완 ↔ 오른쪽 상완)는 **gap_level · priority 를
같은 값으로** 보내세요. 좌우가 실제로 뚜렷하게 다를 때만 등급을 다르게 매깁니다 —
없는 차이를 지어내지 마세요.

⚠️ **다만 문장(assessment)의 주어는 «양쪽» 이 아니라 «그 카드의 부위» 입니다.**
   사용자는 «왼쪽 상완» 카드를 눌러서 그 카드만 보고 있는데 "양쪽 상완은…"
   이라고 하면 지금 뭘 보고 있는지가 흐려집니다. 좌우를 묶어 부르는 것은
   **종합 진단의 몫**입니다.

⚠️ 반대쪽 부위의 관찰이나 수치를 이 카드로 끌어오지 마세요. 인바디는 좌우가
   다른 값이라, 문장을 복사하면 오른쪽 카드에 왼팔 수치가 실려 나갑니다
   (실측 2026-08-20).

# 출력 형식

JSON 하나만 반환합니다. `<>` 는 채워 넣을 자리 표시입니다 —
**자리 표시나 이 명세의 문장을 출력에 그대로 복사하지 마세요.**

{{
  "observed": {{
    "reference": "<레퍼런스의 옷차림과 덮는 범위. 프레임에 안 들어온 부위도 함께>",
    "user": "<사용자의 옷차림과 덮는 범위. 위 표를 근거로 어디가 드러났는지 함께>"
  }},
  "parts": [
    {{
      "class_name": "<부위 목록에 있는 이름 그대로>",
      "seen": {{
        "user": "<사용자 사진에서 이 부위의 **몸**이 어떻게 보이는지. 안 보이면 왜>",
        "reference": "<레퍼런스 사진에서 같은 부위>"
      }},
      "differences": ["<두 사진을 견줘 **눈으로 본** 차이 0~2개>"],
      "assessment": "<현재와 목표를 나란히 놓고 무엇이 어떻게 다른지.
                     분량은 §분량 표를 따르세요. 판단 불가면 왜 못 봤는지만>",
      "gap_level": "<NONE|SLIGHT|MODERATE|SIGNIFICANT, 판단 불가면 null>",
      "priority": <1~5 정수 또는 null>,
      "confidence": "<HIGH|MEDIUM|LOW>",
      "blocked_reason": "<못 본 이유. 봤으면 null>"
    }}
  ]
}}

⚠️ **요청받은 부위를 하나도 빠뜨리지 마세요.** 못 본 부위도 blocked_reason 을
   채워 배열에 넣습니다 — 빠지면 사용자 화면에서 그 부위만 조용히 사라집니다.
⚠️ differences 는 **본 것만** 적습니다. 확인 못 했으면 빈 배열 `[]` 입니다.
⚠️ `seen` 을 **먼저** 채우세요. 두 칸을 다 적고 나면 그 부위를 비교할 수 있는지,
   무엇이 다른지가 이미 정해집니다 — 그걸 보고 나머지 칸을 채웁니다.
   `seen.user` 가 "티셔츠를 입음" 처럼 **옷 이야기로만** 채워지면 부위를 안 본
   것입니다. 그 부위에서 **몸이 어떻게 보이는지**를 적으세요."""


def build_part_comparison_prompt(
    parts: list[dict[str, Any]],
    inbody: dict[str, Any] | None,
) -> str:
    """부위별 비교(세그 없음) 사용자 메시지. 이미지 2장은 호출부가 앞에 붙인다.

    Args:
        parts: body_part 마스터 행 (class_name / name_ko). ⚠️ 부위 목록은 DB 가
               유일한 출처다 — 여기서 만들지 않는다 (F08 과 같은 규약).
    """
    lines = [
        "# 부위 목록 — 이 부위들을 각각 비교하세요",
        "",
        "두 사진에서 아래 위치를 직접 찾아 비교합니다. 색으로 칠해진 그림은 없습니다.",
        "",
    ]
    for p in parts:
        name = p["class_name"]
        ko = p.get("name_ko") or name
        where = _LOCATION.get(name, ko)
        lines.append(f"- `{name}` ({ko}) — {where}")
        # ⚠️ 볼 지점을 부위마다 다르게 준다. 이게 없으면 부위가 달라도 같은
        #    문장이 나온다 (_LOOK_AT 주석의 실측 참고).
        look = _look_at(name)
        if look:
            lines.append(f"    볼 것: {look}")

    lines += [
        "",
        f"⚠️ 위 {len(parts)}개 부위가 **전부** 출력 배열에 있어야 합니다.",
        "   못 본 부위는 blocked_reason 을 채워 넣으세요 (빼지 마세요).",
        "",
        "# 인바디",
        "",
        _inbody_block(inbody),
        "",
        "# 요청",
        "",
        "두 사진을 부위별로 비교해 JSON 으로 반환하세요.",
        "먼저 부위마다 «볼 수 있는가»를 판단하고, 볼 수 있는 부위만 등급을 매기세요.",
        "각 카드는 **그 부위에서만 할 수 있는 말**로 쓰세요 — 문형을 돌려쓰지 마세요.",
    ]
    return "\n".join(lines)
