"""2차 검사 프롬프트 — 이 두 장으로 뒤에 오는 계산이 성립하는가.

⚠️ **확인 항목은 사진을 보고 만든 게 아니라 계산에서 유도한 것이다.**

    첫 판(2026-08-14 오전)은 실제 사진을 돌려보며 규칙을 하나씩 붙였다.
    그러면 "왜 하필 이 규칙들인가"에 답할 수 없고, 새 유형이 올 때마다 규칙이
    늘어난다. 빠뜨린 게 있는지도 알 수 없다.

    지금은 반대로 간다. **이 사진으로 무슨 계산을 하는지 적고, 각 단계가 깨지는
    지점을 세어서** 확인 항목을 만든다. 단계 수만큼만 나온다.

이 뒤에 실제로 일어나는 계산 (담당 B 확인 완료 2026-08-14)
    ① Sapiens 가 픽셀을 부위별로 분류한다
    ② area_share = 부위 픽셀 / **인물 전체 픽셀** (part_group != 'OTHER')
       ⚠️ 자는 어깨~골반이 아니라 **인물 전체**다. 머리·발도 분모에 들어간다.
    ③ diff_pct = (사용자 share − 레퍼런스 share) / 레퍼런스 share
       ⚠️ **상대 비교다.** 두 사진이 비슷하게 잡혔으면 분모 오차가 상쇄된다.
    ④ VLM 이 이미지 4장(원본 2 + 부위 오버레이 2)으로 근육 윤곽·선명도·
       실루엣·좌우 균형·**크기 차이까지 전부 이미지로** 판단한다.
       ⚠️ ②③ 의 수치는 VLM 에게 주지 않는다 (2026-08-14 폐기). 두 사진은
          각도·거리·자세가 달라 픽셀 크기 비교가 성립하지 않기 때문이다.
          ③ 은 framing_bias 탐지와 대표부위 선정에만 쓰인다 — 그래서 아래
          C3(배율 균일성)은 여전히 필요하다.
    ⑤ 부위별 clothing_ratio(옷에서 흡수한 비율)를 프롬프트에 넣어, 모델이
       "이 부위는 옷 위 실루엣"이라고 스스로 판단을 유보할 수 있게 한다.

각 단계가 깨지는 조건 → 확인 항목
    ① 이 깨진다 ─ 픽셀을 분류할 수 없다
        C1  인물이 정확히 한 명인가      아니면 누구의 픽셀인지 정해지지 않는다
        C2  몸과 배경의 경계가 보이는가   아니면 분류가 근거 없는 값이 된다
    ③ 이 깨진다 ─ 상쇄가 안 된다
        C3  배율이 부위마다 균일한가      극단 근접이면 가까운 쪽만 커진다
    ④ 가 깨진다 ─ 비교할 짝이 없다
        C4  비교 대상 부위가 프레임 안인가
        C5  옷에 묻히지 않은 부위가 하나라도 있는가

    **다섯 개가 전부다.**

⚠️ 첫 판에서 **빠진 항목** (근거가 없어져서)
    "어깨선과 골반선이 프레임 안에 있는가" 를 넣었었다. 어깨~골반으로 크기를
    맞춘다고 **짐작**했기 때문인데, 실제 자는 인물 전체 픽셀이다.
    (어깨~골반은 자세 점수(1차 검사)의 자다. 거기서 이미 보고 있다 — 몸통이
     안 보이면 framing_score 가 0 이 되어 1차에서 걸린다.)

⚠️ 첫 판에서 **약해진 항목**
    옷 조건을 "몸통·팔·다리 중 하나라도 묻히면 반려"로 잡았었다. 지금은
    **전부 묻혔을 때만** 반려한다. ⑤ 가 생겼기 때문이다 —
    부분적으로 가려진 부위는 clothing_ratio 가 프롬프트에 실려 모델이 부위별로
    판단을 유보한다. 다리만 묻힌 사진을 통째로 반려하면 **멀쩡한 상체 진단까지
    못 보게 된다.** 부위가 너무 적으면 세그 뒤에 retake_recommended 가 잡는다.

⚠️ **일부러 넣지 않은 것 — 선명도 게이트** (담당 B 판단, 동의)
    ④ 가 근육 윤곽을 읽으므로 "질감이 보이는가"를 넣고 싶어진다. 넣지 않는다.
      * 임계값 근거가 없다. 지금 정하면 clothing_ratio > 0.5 때와 같은 실수다.
      * **게이트는 전역인데 문제는 국소다.** 몸통은 선명한데 종아리만 흐린
        사진이 흔하다. 전역 컷오프는 이걸 못 나눈다.
      * 이미 부위별 blocked_reason 이 그 층에서 처리한다.
    C2 는 "질감이 보이는가"가 아니라 **"경계가 잡히는가"**다. ① 이 깨지는 선까지만 본다.

⚠️ **잘림은 여기서 안 잡는다** (2026-08-14 확인)
    1차 검사의 framing_score 가 bbox 겹침 → 몸통 길이 비율로 바뀌면서 잘림을
    못 잡게 됐다(몸통 길이는 무릎까지 잘려도 1.0). 게이트 변경 자체는 옳다 —
    옛 방식은 팔다리 움직임을 프레이밍 문제로 오인해 물러서도 고칠 수 없는
    안내를 띄웠다.
    ③ 이 상대 비교라 **양쪽이 비슷하게 잘리면 상쇄**되고, 한쪽만 잘린 경우는
    담당 B 의 framing_bias 지표(9부위 diff_pct 중앙값 + 같은 부호 부위 수)가
    탐지한다. 균일 잘림은 9/9 같은 방향, 실제 체형 차이는 5/9 로 갈린다.
    → 여기서 또 보면 이중 판정이 된다. C4(부위가 프레임 안인가)만 본다.

실측 (2026-08-14, gpt-4o, temperature=0, 긴 변 768px)
    사진 6장 × 3회 = 18회. **전부 기대와 일치하고 전부 같은 답.** 1.3~2.1초.
      머리 잘림(목 아래만)         → 통과
      어둡지만 외곽선은 보임        → 통과
      상반신만 (다리 프레임 밖)     → **통과**  ← 유도로 바뀐 것
      흐림                         → 반려 BLURRY
      헐렁한 옷 / 데님재킷+슬랙스   → 반려 LOOSE_CLOTHING
    재현: python scripts/verify_photo_screening.py --repeat 3
    ⚠️ 아래 상대화(7번 신설) 이후 재실측 필요 — 위 표는 상대화 이전 판 기준이다.

⚠️ 레퍼런스 상대화 (2026-08-15)
    기획 확인: **사용자 사진은 레퍼런스만큼만 나오면 된다.** 레퍼런스가 상체만이면
    사용자도 상체만으로 충분하고, 전신이면 전신이어야 한다.
    같은 날 전제 문구도 사실대로 고쳤다 — 도입부의 "서비스가 미리 준비한 사진"은
    실제 서비스와 달랐다(**레퍼런스도 사용자가 업로드한다**). 거짓 전제를 모델에
    주지 않기 위해 수정. 레퍼런스 자체의 품질 검사는 이 프롬프트가 아니라
    **레퍼런스 업로드 시점의 별도 검사**로 다루는 것이 방향이다(아직 미구현 —
    잘못을 알려야 할 상대는 레퍼런스를 올리던 그 순간의 사용자이기 때문).
    종전 판은 사용자 사진만 절대 기준으로 봐서, "레퍼런스는 하체인데 사용자는
    상체" 같은 **비교 불능 쌍이 통과**했다 — 세그(GPU)와 저장을 지불한 뒤에야
    교집합 계산(INSUFFICIENT_PARTS)에서 걸렸다. 가장 싼 단계에서 거르도록
    1단계에 레퍼런스 관찰(굵기 3항목)을 추가하고, **7번(비교 가능 부위 0개 →
    PART_MISMATCH)** 을 신설했다. 유도로는 ④(비교할 짝이 있는가)를 레퍼런스
    기준으로 완성한 것이다. (7번이 비어 있던 건 이전 판에서 규칙을 빼며 번호를
    안 당긴 자리다 — 이번에 채워져 1~9 가 연속이 됐다)
    ⚠️ 두 장을 같이 관찰하게 되면서 "같은 사람·같은 배경이면 섞어 본다" 함정
       (아래 실패 3번)의 노출이 커졌다 — 레퍼런스 관찰은 굵기 3항목으로 최소화했다.
    ⚠️ 하체 전용 레퍼런스는 여기를 고쳐도 **1차(자세)가 먼저 막는다** — 자세·거리
       산식의 자가 어깨~골반이라 몸통 없는 사진은 성립하지 않는다(HIP_KNEE 미구현).
       상체·전신 레퍼런스까지가 현재 지원 범위다.
    ⚠️ 남은 배관(별도 작업, 프롬프트 밖):
       * observed 를 사진 행에 저장 → 하류(세그·진단)가 부위별 판단 유보에 사용
       * 적중률 실측 — (2차 관찰) vs (세그 유효/무효) 쌍으로 "이 검사가 사피엔스
         결과를 예측하는가"를 숫자로 만들기
       * 강화판: 레퍼런스 세그 결과(유효 부위 목록)를 서버가 직접 주입 — VLM 관찰
         보다 정확하나, 레퍼런스 세그 미완료 시점 처리가 필요해 뒤로 미룸

⚠️ 유도판으로 옮기며 **실제로 바뀐 판정**
    "상반신만 나온 사진"이 반려 → 통과가 됐다. 몸통과 팔이 프레임 안에 있으므로
    ④ 의 짝이 있고, 그 부위들의 진단은 성립한다. 다리가 없다고 통째로 막으면
    가능한 진단까지 못 보게 된다. 부위 수가 모자라면 세그 뒤 retake_recommended
    가 잡는다 — **판정을 한 층 뒤로 미룬 것이지 없앤 게 아니다.**

⚠️ 이 판을 만들며 두 번 실패했다 (되풀이하지 않도록)
    1. 관찰 항목 이름을 `torso_bare` 처럼 바꿨더니 **관측 자체가 틀렸다.**
       상반신 나체 사진에 torso_bare=false 가 나왔다. 검증된 어휘
       (`torso_shape_visible` = "굵기를 알 수 있는가")를 그대로 두는 게 맞다.
    2. 규칙을 표로 옮겼더니 조건과 실패 사유가 섞여 오판정이 났다.
       번호 붙은 문장 형태를 유지할 것.
    ⚠️ **어휘와 구조는 건드리지 말고, 유도가 요구하는 것만 바꿀 것.**

⚠️ 여기까지 오는 데 고친 것 (같은 실수를 반복하지 않도록)
    1. temperature 를 안 잡아 기본값 1.0 으로 돌았다. 같은 사진이 3회 중 2회
       뒤집혔다. 판정에는 0 을 쓴다.
    2. "판단해라" 형태여서 흔들렸다. **"보이는 것을 먼저 적고, 정해진 순서로
       판정해라"** 로 바꾸니 안정됐다.
       ⚠️ 이건 **일관성**을 위한 구조다. **무엇을 확인할지**(위의 C1~C5)를
          정하는 것과는 별개 문제다. 구조가 안정적이어도 항목이 임의면 소용없다.
    3. ⚠️ 검증용 반려 사진을 **레퍼런스를 잘라 만들면 안 된다.** 같은 사람·
       같은 배경이면 모델이 두 장을 섞어 보고 없는 부위를 "보인다"고 답한다.
    4. 밝기만 낮춰서는 TOO_DARK 를 만들 수 없다. 대비가 같이 줄지 않아
       0.04 배까지 낮춰도 윤곽이 남는다. 실사로만 확인 가능하다.

⚠️ 이 프롬프트는 **B 의 계산이 바뀌면 같이 바뀌어야 한다.** 유도의 입력이
   ①~⑤ 이므로, 정규화 기준이나 진단 입력이 달라지면 조건도 달라진다.

⚠️ mirror_suspected (2026-08-17 추가 · 2026-08-18 배경 갱신)
    갤러리에 올라온 거울 셀카는 좌우가 뒤집힌 채 **에러 없이** 진단까지 흘러간다
    (images.flip_horizontal 주석 참고). 그래서 2차 검사에 **기록용 관찰**을 하나
    얹었다. 반려 규칙이 아니다 — 규칙표(1~9)는 그대로다.
    ⚠️ 종전에는 사용자 신고(is_mirrored 체크박스)의 누락을 찾는 용도였는데,
       2026-08-18 에 그 체크박스를 없앴다. 이제 거울 셀카를 걸러낼 장치가 아예
       없으므로 이 관찰의 가치는 오히려 커졌다 — 실제 빈도가 얼마인지, 방향
       대응을 되살릴 필요가 있는지 판단할 유일한 근거다.
    observed 가 사진 행에 저장되기 시작하면(위 "남은 배관") 조회로도 걸러낼 수 있다.
"""

# ⚠️ 프롬프트 본문은 DB(prompt_version)에 있다 (2026-09-11, #164) — 'photo_screening.system' · 'photo_screening.user'.
#    보기: python scripts/prompt_version.py show photo_screening.system
#    고치기: python scripts/prompt_version.py new photo_screening.system <파일> --note "…" (배포 없음)


#: ⚠️ 이미지는 [레퍼런스, 사용자] 순서로 넣는다. 프롬프트가 그 순서를 전제한다.

