# 최종 수정사항 — 2026-08-16 검토 위원회

> 회의 전문·반대 의견: [review-council-20260816.md](review-council-20260816.md)
> 5인 페르소나 2라운드 심의. **의장 판정 기준: "점수에 영향 있나"** (목표는 해커톤 우승)
> 전제: **남은 시간 하루**

---

## 0. 이 문서를 읽는 법

- **합의 P0** = 2명 이상이 독립적으로 지목 + 2라운드에서 반박 없이 살아남음
- 시간은 **의장 추정이 아니라 위원이 직접 재본 값**
- 모든 항목에 `파일:줄` 근거가 있다. 없으면 그 항목은 아직 확정이 아니다
- **✅ 표시는 의장이 코드로 직접 재현한 것**

---

## 1. 합의 P0 — 코드 (총 35분)

이 다섯 개를 먼저 한다. **순서대로.**

| # | 항목 | 파일:줄 | 시간 | 지지 위원 |
|---|---|---|---|---|
| 1 | **main 동결** | `.github/workflows/deploy.yml:17-18` | 5분 | 심사위원장·테크리드·SRE·운동생리학 |
| 2 | **OpenAI timeout 4곳** | `vlm.py:137` `coach_chat.py:354` `ocr.py:127` `routine.py:530` | 10분 | **전원 (만장일치)** |
| 3 | **초보 필터 재계산** ✅ | `exercise_catalog.py:352` | 5분 | 테크리드·윤리·운동생리학 |
| 4 | **단측 운동 우선순위** | `exercise_catalog.py:359-366` | 5분 | 테크리드·운동생리학 |
| 5 | **검증 두 줄 추가** | `scripts/verify_routine_rules.py` | 10분 | 운동생리학·테크리드 |

### 1번 — main 동결 (5분, 가장 값싼 보험)

`deploy.yml`의 `push:` 블록을 지우고 `workflow_dispatch`만 남긴다. 발표 후 되돌린다.

**왜 1번인가:** 롤백 절차가 없는 상태(확인됨 — `docs/`·`.github/` 전체에 없음)에서 오늘 하는 모든 수정이 곧 위험이다. 이 5분이 나머지 전부를 보호한다. SRE가 신규 `docker-compose.rollback.yml`을 제안했다가 *"발표 전날 배포 경로에 파일을 추가하는 것 자체가 새 리스크"*라며 철회하고 이걸 택했다.

> ⚠️ **2·3·4·8번을 먼저 반영해 마지막 배포를 끝낸 뒤 동결한다.** 동결이 먼저면 아무것도 못 올린다.

### 2번 — OpenAI timeout (10분, 만장일치 최우선)

```python
# 4곳 전부
client = AsyncOpenAI(api_key=settings.openai_api_key, timeout=60, max_retries=1)
```

**현재 상태 (검증됨):** 네 곳 모두 `timeout=` 인자가 없다. openai 3.0.0 기본값 read=600s × max_retries=2 → **한 호출이 최대 1800초** 매달린다. `POST /coach/chat`은 `asyncio.wait_for`도 없어 **30분 스피너**가 가능하다.

**하나로 셋을 산다** (테크리드):
- 코치챗 무한 스피너 차단
- 이벤트 루프 블로킹 최악값을 600초 → 60초로 (C3의 실질 해소)
- 요금 2배 창(B3)을 닫는다 — 잡이 900초를 넘길 경로가 사라진다

### 3번 — 초보 필터 (5분) ✅ 의장 재현 완료

```python
# app/services/exercise_catalog.py:352
and (not beginner_only or is_beginner_safe(e.get("name_en") or "", e.get("exercise_type")))
```

같은 모듈 안 함수라 import 불필요. **DB 재시드 불필요** (런타임은 JSON을 읽는다).

**의장이 직접 재현한 결과 — 200종목 중 12건이 통과하고 있다:**

```
Jump Squat · Chin-ups · Wide Grip Pull-Up · Chest Dip
Pull-up with Bent Knee between Chairs · Pull-up · L-Sit on Floor · Pull up
Dumbbell Jumping Squat · Commando Pull-up · L-Pull-up · Chin-Up
```

운동생리학 위원 실측: **체지방 40% 여성 CUT 3일 루틴 가슴 슬롯에 "체스트 딥"이 실제 배정됐다.**

**원인** — 두 소스가 다른 잣대를 쓴다:
- JSON 플래그는 `scripts/fetch_exercisedb.py:74 ADVANCED_KEYWORDS`로 찍힘 (dip·pull up **없음**)
- 엄격한 `BEGINNER_UNSAFE_TERMS`(`exercise_catalog.py:252-266`)는 `seed_exercise_catalog.py:67`에서 **DB에만** 적용
- 런타임 `load_catalog()`(`:273-281`)는 **JSON**을 읽는다

`exercise_catalog.py:109-111`이 *"수집본 JSON 의 값을 믿지 않는다"*고 선언해 놓고 코드가 정반대로 동작한다.

> **기본값 `True`를 뒤집는 수정은 오답이다.** JSON 200행 전부 키가 있어서 기본값은 한 번도 발동하지 않는다 (의장 확인).

### 4번 — 단측 운동 우선순위 (5분)

`rank` 튜플에서 `not (prefer_single_side and single)`을 **5번째 → 2번째**(`not muscle_hit` 다음)로 옮긴다.

**커밋 `34c7349`(좌우 불균형 = 순서 교정)가 배선은 맞는데 실측 발동률이 0이다.** 앞의 4키(저충격·주동근·장비·복합)가 순서를 이미 확정해서 단측 여부까지 도달하지 않는다. `prefer_single_side=False`일 땐 항상 `True`라 기존 정렬에 영향 없다.

### 5번 — 검증 두 줄 (10분, 3·4번의 증명)

`verify_routine_rules.py`가 **PASS인데 기능은 안 돈다.** `:136`이 `routine._side_order_note("LEFT")`를 직접 호출해서 헬퍼의 문자열 포맷만 증명하고, 프로덕션 배선(`routine.py:442`)을 안 탄다.

```python
# rule_asymmetry_is_order_not_volume() 끝에 — 산출물에 어서션을 건다
plan = asyncio.run(build_routine(4, None, ["Left_Upper_Leg", "Torso"], ["Left_Upper_Leg"]))
check("순서 안내가 실제 운동 note 에 붙는다",
      any("부터 시작" in e.get("note", "") for d in plan["days"] for e in d["exercises"]))

# 신규 rule — 초보 필터의 런타임 결과
check("후보에 초보 부적합 없음", not [
    x for s in ec.SLOT_BODY_PARTS for x in ec.candidates_for_slot(s, cat)
    if not ec.is_beginner_safe(x["name_en"], x["exercise_type"])])
```

**지금 넣으면 둘 다 FAIL이 정상이다.** 3·4번을 고치면 GREEN이 되고, **그때부터 무대에서 "검증 통과"를 말할 수 있다.**

---

## 2. 합의 P0 — 문서·설정 (총 43분)

| # | 항목 | 파일 | 시간 |
|---|---|---|---|
| 6 | DISCLAIMER에 외부 전송 고지 | `app/schemas/analysis.py:144-147` | 3분 |
| 7 | `score_source` 문서·스키마 정합 | 문서 3곳 + 스키마 2곳 | 30분 |
| 8 | `stop_grace_period: 120s` | `docker-compose.yml` | 10분 |

### 6번 — DISCLAIMER (3분)

```python
DISCLAIMER = (
    "본 분석은 사진 기반 추정이며 의학적 조언이 아닙니다. "
    "통증이 있거나 지속되면 운동을 중단하고 전문가와 상담하세요. "
    "업로드한 사진과 인바디 결과지는 분석을 위해 외부 AI(OpenAI)로 전송되며, "
    "계정을 삭제하면 서버에서 모두 삭제됩니다."
)
```

상수 하나라 두 화면에 동시 반영된다. **"답변 불가" 질문 하나를 3분에 지운다.**

### 7번 — score_source 정합 (30분) ✅ 의장 확인 완료

**코드는 이미 옳다. 문서만 반대로 말한다.**

```
코드(진실):  vlm.py:349 → ScoreSource.RULE
             scoring.py:1 "결정론적 규칙 합산"
             vlm.py:416 "LLM 이 점수를 보내와도 버린다"

문서(거짓):  schemas/analysis.py:81   score_source: str = "VLM"   ← 기본값
             routes/analysis.py:203    or "VLM"                    ← 폴백
             llm-strategy.md:235       "VLM 직접 출력만 쓴다"
             api-spec-v2.md:606, :882  "VLM 고정"
             hackathon-qa.md:305-312   §5 (철 지난 단락)
```

`hackathon-qa.md:102`는 *"점수는 AI가 만들지 않습니다. 공식이 만듭니다"*라고 하고, 같은 문서 305줄은 반대로 말한다.

**추가 발견 (테크리드):** 종합이 끝나기 전 화면에는 `score_source: "VLM"`이 뜬다. **"점수는 규칙 기반입니다"라고 발표하는 동안 화면이 VLM이라고 말한다.**

**별도 항목:** `vlm.py:321`은 **실패 경로에서 `ScoreSource.VLM`**을 쓴다. 성공은 RULE, 실패는 VLM. 문서가 아니라 코드 자체의 불일치다.

### 8번 — stop_grace_period (10분)

```yaml
services:
  api:    { stop_grace_period: 120s }
  worker: { stop_grace_period: 120s }
```

`run.py:95-98`에 graceful stop 코드가 **이미 있는데** compose 설정이 없어서 무용지물이다. 기본 10초 뒤 SIGKILL, VLM 잡은 30~90초. 배포마다 진행 중 잡이 죽고 → 15분 갇혔다가 회수되고 → **Vision 호출이 한 번 더 나간다.**

> main 동결로도 안 막힌다. 마지막 배포 한 번은 반드시 해야 하기 때문이다.

---

## 3. T-24h 운영 (총 ~1시간 45분)

| # | 항목 | 시간 | 비고 |
|---|---|---|---|
| 9 | **백업 세션 + 화면 녹화** | 60분 | 코드로 못 막는 유일한 P0 |
| 10 | **M1~M8 실측** | 30분 | A5 + C6 통합 (중복 30분 반납) |
| 11 | **EC2 `.env` 실제 값 확인** | 5분 | ⚠️ **SSH 키 필요** |
| 12 | 삭제 절차 런북 한 줄 | 10분 | D6 |

### 9번 — 백업 세션 (60분, 대안 없음)

**`USE_MOCK`은 세그멘테이션을 덮지 않는다** ✅ 의장 검증 완료 — `seg.py`/`segmenter.py`에 mock 분기 **0건**. mock은 LLM 경로 5곳뿐이다.

`docs/hackathon-qa.md:339`가 장애 대비책으로 적어둔 **"`USE_MOCK=true` 전환 절차 숙지"는 존재하지 않는 대비책이다.** 데모를 죽일 확률이 가장 높은 것(RunPod GPU, 콜드스타트 수 분)이 정확히 mock으로 안 덮이는 유일한 경로다.

**절차:**
1. **팀원 본인 사진으로** 시연 전용 user 생성 (타인 데이터 아님 → 보관 근거 있음)
2. `web/e2e-test.html` API 칸에 `https://api.refit.live` 입력 후 전 구간 완주
3. `user_id` / `session_id`를 **종이에** 기록
4. 진단→루틴 화면 **녹화**
5. 발표 종료까지 **삭제 금지**

> ⚠️ **`scripts/smoke_full_flow.py`를 쓰지 마라.** `--keep` 플래그가 없고 `:427-434`가 끝나면 DB·Storage를 무조건 지운다. 백업이 날아간다.

### 10번 — 실측 (30분)

전 구간 소요시간이 **문서 어디에도 없다.** 3분 대본을 이 숫자 없이 짤 수 없다. `scripts/watch_job_timing.py`가 이미 있다. 각 3회, 중앙값.

| # | 항목 | 실패 판정선 |
|---|---|---|
| M1 | 세그멘테이션 1장 | > 15초면 무대 각본 변경 |
| M2 | 스크리닝 1회 | timeout 값의 근거 |
| M3 | F08 부위 진단 | > 40초면 스피너 문구 필요 |
| M4 | F09 종합 진단 | — |
| M5 | 루틴 생성 (LLM) | 폴백 완주 시간도 별도 |
| M6 | 루틴 생성 (FALLBACK) | **이미 확보** — 체감 즉시 |
| M7 | **업로드→활성 루틴 전 구간** | 이 숫자가 발표 슬라이드에 들어간다 |
| M8 | 코치챗 1턴 | > 20초면 스트리밍 필요 |

> M6이 확보돼 있다 = **LLM이 죽어도 루틴은 나온다는 게 실측으로 증명된 상태**다. 무대에서 말할 가치가 있다.

### 11번 — EC2 `.env` 확인 (5분) ⚠️ 미해결 사실

```bash
ssh -i ~/.ssh/refit-ec2 ubuntu@15.165.25.201 "grep -E 'PHOTO_SCREENING_ENABLED|CORS_ORIGINS' ~/KingDreamTree-Backend/.env"
```

로컬 `.env:60`은 `PHOTO_SCREENING_ENABLED=false`, `.env.example:68`은 `true`다 (의장 확인). **하지만 EC2의 `.env`는 서버에서 직접 만든 별개 파일이고 아무도 실제 값을 모른다.** `/health`는 `mock`만 노출해서 원격으로 알 방법이 없다.

**이 항목이 SSH 키 확보에 걸려 있다.** 팀원 요청 1번이 여기서 실제 공정에 물린다.

---

## 4. 시간이 남으면 (우선순위 순)

| # | 항목 | 시간 | 근거 |
|---|---|---|---|
| 13 | **문제 정의 문서** | 2h | 11/25로 전 항목 최저. **배점상 유일하게 두 자릿수 상승 여지** |
| 14 | 진단 문구 "부족합니다" 제거 | 15분 | **윤리가 아니라 정확성 수정** — 아래 |
| 15 | 저체중 하한 (E8) | 10분 | ⚠️ 2라운드 신규, 교차 검토 미완 |
| 16 | 부상·통증 입력 화면 (E2) | 1h | 프론트 왕복 — 실패 시 발표 대사로 전환 |

### 14번이 왜 "정확성 수정"인가

진단이 산출하는 값은 `gap_level` — **레퍼런스 대비 격차 등급**이지 결핍 판정이 아니다. `scoring.py:26-29`가 스스로 *"이 점수는 격차 크기의 사상이지 유사성의 존재 여부가 아니다"*라고 썼다. **"부족합니다"는 4단계 서열 척도를 절대 결핍으로 번역한 것이고, 리포가 가진 근거를 넘는 주장이다.**

```
X "왼쪽 상완이 목표 체형보다 부족합니다"
O "왼쪽 상완은 목표 체형까지 더 키울 여지가 있습니다"
```

단서: `blocked_reason`이 있는(옷·판단 불가) 부위엔 "늘릴 여지" 문구도 붙이면 안 된다. 안 본 부위에 대한 진술이 된다.

### 15번 — 저체중 경로 (검토 미완, 사실만 확인됨)

✅ 의장 확인: `routine_mode.py`에 **하한이 없고**, `app/`·`scripts/`·`docs/` 전체에 저체중·섭식 관련 가드가 **0건**이다.

```
routine_mode.py:96   is_cut = pct >= cutoff          ← 하한 없음
   체지방 8% · BMI 16 → BALANCE (감량은 안 시킴. 여기까지는 옳다)
vlm.py:423           priority_parts 3개 그대로
routine_templates.py:274,279  근육군당 +4세트, 좌우 둘 다면 ×2 = +8
```

마른 사용자일수록 격차가 크게 나와 **priority_parts 3칸을 다 채우고 볼륨 가중을 최대로 받는다.** "전 부위 부족" 문구 + 최대 볼륨이 같은 방향으로 증폭된다.

> ⚠️ **이 항목은 2라운드에 신규 추가돼 다른 위원의 검토를 못 받았다.** 실행 전 재검토 필요.

---

## 5. 하지 않기로 한 것 (전부 근거 있는 배제)

| 항목 | 배제 사유 | 판정 |
|---|---|---|
| `docker-compose.rollback.yml` 신규 | *"발표 전날 배포 경로에 파일 추가 = 새 리스크"* | **SRE 자진 철회** |
| B3 하트비트 (`started_at` 갱신) | B2가 900초 도달 경로를 없애 창이 닫힌다 | 해커톤 이후 |
| C3 이벤트 루프 스레드풀 이관 | 무대 규칙 "업로드 한 명씩"이 코드보다 싸고 안전 | 발표 후 |
| `SIGNED_URL` 3600→300 | 프론트 재발급 구현 미확인 + D6(발표 후 삭제)가 더 싸게 덮음 | **윤리 자진 철회** |
| E3 CUT 서킷 휴식 60→75초 | *"파라미터 보수성 논쟁을 사실 결함과 같은 등급에 두면 안 된다"* | **운동생리학 자진 철회 (P1)** |
| A3 MediaPipe CDN 로컬 번들 | 와이파이가 죽으면 `api.refit.live`도 못 간다 (SRE) + C1과 중복 (윤리) | **P1** — bypass 제거 20분만 |
| 세그멘테이션 mock 추가 | 가짜 라벨맵·통계까지 지어내는 신규 코드. 하루에 넣을 크기 아님 | 배제 |
| pytest 래핑 **단독** | 커버리지 0 증가에 "CI 그린" 권위만 붙는다 | **5번과 같은 커밋에서만** |
| 나이 확인 서버 게이트 | 시연 대상은 발표자 본인. 체크박스로 충분 | P1 |
| `PHOTO_SCREENING_ENABLED` 켜기 (D5) | 반려 사유 9개에 나이·타인 사진·부적절 **전부 없음** | **윤리 자진 철회** |

---

## 6. 발표 대사 정정 — 지금 말하면 무너지는 문장 5개

| ❌ 지금 준비된 말 | ✅ 정확한 말 |
|---|---|
| "테스트 12개 통과" | "불변식 12종이 스크립트로 고정돼 있고, **CI에는 아직 안 물려 있습니다**. 커버리지 지표가 아니라 조용히 깨지는 지점을 못 박는 용도입니다" |
| "점수는 AI가 아니라 공식이 냅니다" | **7번을 먼저 고친 뒤에만** 말할 수 있다. 지금은 문서 3곳이 반대로 적혀 있다 |
| "장애 시 USE_MOCK으로 전환합니다" | **삭제.** 세그를 안 덮는다. 대신 "백업 세션과 녹화를 준비했습니다" |
| "LLM 호출 4회, 1인당 83원" | "**6회**"(2차 스크리닝 + 코치챗 포함). 83/80원 통일. **GPU 비용 한 줄 추가** |
| "5겹 방어" (데모에선 자동 bypass) | `e2e-test.html:355`가 관문 미달 시 체크박스를 자동으로 켜고 로그에 "⚠️ 관문 무시"를 찍는다. **먼저 제거** |

### "못 한 것 3개" 선제 고백 — 최종본

2라운드에서 **1번이 연령 → 부상 스크리닝으로 바뀌었다.** 심리적 위해보다 신체적 위해를 먼저 인정하는 순서가, 위험의 크기를 구분할 줄 안다는 신호가 된다.

> **한 것 셋** — ① 지워달라면 실제로 지웁니다(soft delete 안 씀, Supabase 목록이 100건에서 조용히 잘리는 것까지 확인해 페이징으로 훑습니다. 안 그러면 101번째 사진부터 남습니다). ② 점수는 AI가 아니라 규칙이 냅니다. ③ 안전은 배지가 아니라 코드가 강제합니다.
>
> **못 한 것 셋** — ① **운동 전에 부상 이력을 묻지 않습니다.** ACSM 사전 참여 스크리닝의 기본인데, 통증은 운동을 *한 뒤에* 묻게 돼 있어 무릎 수술 이력이 있는 분도 1주차에 스쿼트를 받습니다. 받는 배관은 이미 코드에 있고 입력 화면 하나만 앞에 붙이면 됩니다. ② 연령 확인이 없습니다. ③ 레퍼런스 사진의 권리를 확인하지 않습니다.
>
> 이 중 **외부 전송 고지는 오늘 문구부터 넣었고**, 시연 데이터는 발표가 끝나면 오늘 안에 삭제합니다 — 절차도 런북에 있습니다.

---

## 7. 총점

| 항목 | 1R | 2R | 위 목록 반영 시 |
|---|---|---|---|
| 문제 정의·임팩트 | 11 | 11 | 13번 하면 +6~8 |
| 기술 완성도 | 19 | **15** | 1~5번 +3~4 |
| 데모 완성도 | 9 | **7** | 9~11번 +5~6 |
| 차별성 | 11 | 11 | — |
| 발표·방어 | 7 | **6** | 6번 + 대사 정정 +2~3 |
| **합계** | **57** | **50** | **68~75** |

**교차 검토가 단독 검토보다 7점 더 깎았다.** 심사위원장 본인의 말: *"팀이 하루 사이에 나빠진 게 아니라 내 1라운드 해상도가 낮았다는 뜻이다."*

우승권 진입선(75)은 **13번(문제 정의 문서)의 완성도가 결정한다.**
